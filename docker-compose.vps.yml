# VPS Postgres only — never bind 5432 (Qchat) or publish 0.0.0.0.
# Usage: docker compose -f docker-compose.vps.yml --env-file /etc/paymentgate/postgres.env up -d
services:
  postgres:
    image: postgres:16-alpine
    container_name: paymentgate-postgres
    restart: unless-stopped
    ports:
      - "127.0.0.1:5433:5432"
    env_file:
      - /etc/paymentgate/postgres.env
    environment:
      POSTGRES_USER: paymentgate
      POSTGRES_DB: paymentgate
    volumes:
      - paymentgate_pgdata:/var/lib/postgresql/data
    mem_limit: 512m
    pids_limit: 200
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U paymentgate -d paymentgate"]
      interval: 5s
      timeout: 5s
      retries: 10
    command:
      - postgres
      - -c
      - listen_addresses=*
      - -c
      - shared_buffers=128MB
      - -c
      - max_connections=80
      - -c
      - log_min_duration_statement=2000

volumes:
  paymentgate_pgdata:
