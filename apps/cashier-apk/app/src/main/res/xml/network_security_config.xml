<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug / local API only. Production flavors must use HTTPS (M4-23). -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
