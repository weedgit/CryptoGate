# Prefer monorepo root `.env` (vite envDir). Local override only if needed.
# Product / production builds must omit this or set mainnet (testnet never appears).
# VITE_PAYMENTGATE_CHAIN_ENV=testnet
