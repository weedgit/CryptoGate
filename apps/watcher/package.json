{
  "name": "@cryptogate/watcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chain watcher — owned by Bruce. Sprint 0 scaffold only.",
  "scripts": {
    "build": "node -e \"console.log('@cryptogate/watcher: scaffold — Bruce implements')\"",
    "typecheck": "node -e \"process.exit(0)\"",
    "test": "node -e \"process.exit(0)\"",
    "lint": "node -e \"process.exit(0)\"",
    "start": "node ./src/main.mjs"
  },
  "dependencies": {
    "@cryptogate/domain": "workspace:*"
  }
}
