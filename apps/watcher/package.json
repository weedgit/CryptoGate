{
  "name": "@paymentgate/watcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Chain watcher — owned by Bruce.",
  "scripts": {
    "build": "node -e \"console.log('@paymentgate/watcher: m3 match wire')\"",
    "typecheck": "node -e \"process.exit(0)\"",
    "test": "node --test test/*.test.mjs",
    "lint": "node -e \"process.exit(0)\"",
    "start": "node ./src/main.mjs --loop",
    "start:once": "node ./src/main.mjs"
  },
  "dependencies": {
    "@paymentgate/chain-clients": "workspace:*",
    "@paymentgate/domain": "workspace:*",
    "@paymentgate/matching": "workspace:*",
    "pg": "^8.13.1"
  }
}
