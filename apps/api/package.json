{
  "name": "@cryptogate/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HTTP API — owned by Andrew. Sprint 0 scaffold only.",
  "scripts": {
    "build": "node -e \"console.log('@cryptogate/api: scaffold — Andrew implements')\"",
    "typecheck": "node -e \"process.exit(0)\"",
    "test": "node -e \"process.exit(0)\"",
    "lint": "node -e \"process.exit(0)\"",
    "dev": "node -e \"console.error('Not implemented: Andrew owns apps/api'); process.exit(1)\""
  },
  "dependencies": {
    "@cryptogate/domain": "workspace:*"
  }
}
