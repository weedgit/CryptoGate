openapi: 3.0.3
info:
  title: CryptoGate Phase 1 API
  version: 0.3.5
  description: >
    Contract freeze v0.3.5 (merchant site inherit + Owner override approval).
    Builds on v0.3.4 service bill generation. Source of truth for apps/api,
    apps/payment-page, and apps/cashier-apk. Payment orders are watch-only.
    Service bills are a separate rail — never merge with /orders. Enums match
    @cryptogate/domain. Never return spend keys, full xPub, webhook signingSecret,
    or API key secret on GET.

    Machine clients (security apiKey): send X-Api-Key, X-Timestamp, X-Nonce,
    X-Signature. Canonical string is timestamp LF nonce LF METHOD LF pathAndQuery
    LF sha256Hex(rawBody); signature is hex HMAC-SHA256(apiSecret, canonical).
    Session cookie and public GET /orders/{id}/payment do not sign. Replay and
    skew codes: signature_invalid, timestamp_skew, nonce_replay (401). Rate
    limits: RateLimitPerMinute in domain; 429 rate_limited + Retry-After.
servers:
  - url: /v1
tags:
  - name: auth
  - name: orgs
  - name: orders
  - name: api-keys
  - name: webhooks
  - name: service-bills
  - name: audit
  - name: platform-settings
paths:
  /auth/login:
    post:
      tags: [auth]
      operationId: login
      summary: Password login; returns session token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
      responses:
        "200":
          description: Authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "401":
          description: Invalid credentials
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Login rate limit (10/min per IP)
          headers:
            Retry-After:
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /auth/forgot-password:
    post:
      tags: [auth]
      operationId: forgotPassword
      summary: Request a password-reset email (always opaque success)
      description: >
        Always returns 204 unless PASSWORD_RESET_EXPOSE_LINK=true (dev), which
        may return the reset link in JSON when the email exists. Does not reveal
        whether the address is registered.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              additionalProperties: false
              properties:
                email:
                  type: string
                  format: email
      responses:
        "204":
          description: Accepted (email sent or silently ignored)
        "200":
          description: Dev-only — reset link exposed
          content:
            application/json:
              schema:
                type: object
                properties:
                  resetPath:
                    type: string
                  resetUrl:
                    type: string
        "400":
          description: Invalid email
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /auth/reset-password:
    post:
      tags: [auth]
      operationId: resetPassword
      summary: Reset password with a one-time token from forgot-password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, password]
              additionalProperties: false
              properties:
                token:
                  type: string
                password:
                  type: string
                  minLength: 12
      responses:
        "204":
          description: Password updated
        "400":
          description: Invalid request or password policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Invalid or used token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "410":
          description: Token expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /auth/logout:
    post:
      tags: [auth]
      operationId: logout
      summary: Revoke current session
      security:
        - sessionCookie: []
      responses:
        "204":
          description: Logged out
  /auth/session:
    get:
      tags: [auth]
      operationId: getSession
      summary: Current session and org memberships
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "401":
          description: >
            Not authenticated (`unauthenticated`), or cookie present but TOTP
            step-up is incomplete (`mfa_required`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /auth/profile:
    patch:
      tags: [auth]
      operationId: patchProfile
      summary: Update profile, language, MFA preference, and session timeout
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                displayName:
                  type: string
                  nullable: true
                  maxLength: 120
                locale:
                  type: string
                  enum: [en, zh-CN, zh-TW, ja, ko, vi, th]
                timezone:
                  type: string
                  description: IANA timezone (e.g. Asia/Singapore)
                mfaEnforcement:
                  type: boolean
                  description: User preference — require TOTP enrollment for this account
                sessionTimeoutMinutes:
                  type: integer
                  enum: [15, 30, 60, 120]
                  description: User preference — sliding session TTL
      responses:
        "200":
          description: Updated session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "400":
          description: Invalid profile fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /auth/change-password:
    post:
      tags: [auth]
      operationId: changePassword
      summary: Change password while logged in (clears mustChangePassword)
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [currentPassword, newPassword]
              additionalProperties: false
              properties:
                currentPassword:
                  type: string
                newPassword:
                  type: string
                  minLength: 12
      responses:
        "200":
          description: Updated session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "401":
          description: Not authenticated or wrong current password
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /auth/mfa/enroll:
    post:
      tags: [auth]
      operationId: enrollMfa
      summary: Start MFA enrollment (Owner / Administrator on agent and merchant accounts)
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Enrollment challenge (e.g. TOTP secret + otpauth URL)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MfaEnrollResponse"
  /auth/mfa/verify:
    post:
      tags: [auth]
      operationId: verifyMfa
      summary: Verify MFA code to complete login or enrollment
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MfaVerifyRequest"
      responses:
        "200":
          description: MFA accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Session"
        "401":
          description: Invalid MFA code
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs:
    get:
      tags: [orgs]
      operationId: listOrgs
      summary: List org accounts in the caller’s authorized subtree
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Org list
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/OrgAccount"
    post:
      tags: [orgs]
      operationId: createOrg
      summary: Create org account (Platform / Agent / Merchant / Merchant site rules apply)
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrgRequest"
      responses:
        "201":
          description: Org created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgAccount"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/overview:
    get:
      tags: [orgs]
      operationId: getOrgOverview
      summary: Org detail overview (team, audit, orders, commercial/payout)
      description: >
        One round trip for platform org detail cards. Returns team members,
        recent audit events, scoped payment orders, and merchant commercial or
        agent payout when the caller may read them.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Overview payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgOverview"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}:
    get:
      tags: [orgs]
      operationId: getOrg
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Org account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgAccount"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    delete:
      tags: [orgs]
      operationId: deleteOrg
      summary: Delete an empty agent, merchant, or merchant (site) account
      description: >
        Platform Owner or Administrator may delete an agent / agent_sub / merchant
        org that has no child orgs and no blocking FK dependencies. Parent merchant
        Owner or Administrator may delete an empty merchant (site) under that parent.
        Pass query `cascade=1` to delete the full subtree including members, orders,
        bills, API keys, and webhooks (deepest children first).
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "204":
          description: Deleted
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Has children or linked records
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/delete-preview:
    get:
      tags: [orgs]
      operationId: getOrgDeletePreview
      summary: Preview cascade delete impact for an org subtree
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Delete impact summary
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/status:
    put:
      tags: [orgs]
      operationId: setOrgStatus
      summary: Pause or resume an agent or merchant account
      description: >
        Platform Owner or Administrator may pause (revoke portal access)
        or resume an agent / agent_sub / merchant org. Child orgs are not
        auto-paused.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              additionalProperties: false
              properties:
                status:
                  type: string
                  enum: [active, paused]
                reason:
                  type: string
                  maxLength: 500
                  description: Optional note stored in audit log when pausing
      responses:
        "200":
          description: Org status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgAccount"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/users:
    get:
      tags: [orgs]
      operationId: listOrgUsers
      summary: List org members
      description: >
        Platform operators and org Owner, Administrator, or Viewer may list
        members. Cashier receives 403.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Org members
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/OrgMember"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags: [orgs]
      operationId: inviteOrgUser
      summary: Invite user and assign role
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteUserRequest"
      responses:
        "201":
          description: Membership created (new users include temporaryPassword + invite link)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InviteOrgUserResult"
        "403":
          description: Forbidden (e.g. Cashier cannot manage team)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/users/{userId}/role:
    put:
      tags: [orgs]
      operationId: assignOrgUserRole
      summary: Assign role; only Owner may add/remove Administrator and Viewer
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
        - $ref: "#/components/parameters/userId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role:
                  $ref: "#/components/schemas/UserRole"
      responses:
        "200":
          description: Role updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgMembership"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/users/{userId}/status:
    put:
      tags: [orgs]
      operationId: setOrgUserStatus
      summary: Pause or resume an org membership
      description: >
        Org Owner or Platform Owner may pause (revoke org access) or resume a
        member. Cannot pause the last active Owner or yourself. Pausing revokes
        all sessions for that user so portals reload memberships.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
        - $ref: "#/components/parameters/userId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              additionalProperties: false
              properties:
                status:
                  $ref: "#/components/schemas/MembershipStatus"
      responses:
        "200":
          description: Membership status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgMembership"
        "403":
          description: Forbidden (last owner, self, or not Owner)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Membership not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/users/{userId}:
    delete:
      tags: [orgs]
      operationId: removeOrgUser
      summary: Remove member from org
      description: >
        Org Owner or Platform Owner may delete the membership row. Cannot remove
        the last active Owner or yourself. Revokes all sessions for that user.
        Re-add via invite. Does not delete the users row.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
        - $ref: "#/components/parameters/userId"
      responses:
        "204":
          description: Membership removed
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Membership not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/matching-mode:
    get:
      tags: [orgs]
      operationId: getMatchingModeSettings
      summary: Get merchant matching mode (default B when unset)
      description: >
        Merchant Owner, Administrator, or Viewer (and platform operators / agent
        ancestors) may read. Cashier receives 403. Only merchant and merchant_site
        orgs are valid; other org types return 400. Changing this setting does not
        rewrite open orders — mode is locked onto each order at create.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Current merchant matching mode
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchingModeSettings"
        "400":
          description: Org is not a merchant (or merchant site)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: putMatchingModeSettings
      summary: Set merchant matching mode (Owner / Administrator only)
      description: >
        Labels (product): B Standard, C Amount fingerprint, D Memo tag, S Smart
        address. Cashier and Viewer receive 403. Mode D is only useful when the
        asset/network registry has memoSupported true (USDT Tron is false). Dangerous
        combinations (e.g. Mode S with Mode C) are rejected by packages/matching
        validateMatchingSettings when Andrew wires that check on save. Does not
        rewrite open orders.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMatchingModeRequest"
      responses:
        "200":
          description: Matching mode saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MatchingModeSettings"
        "400":
          description: Invalid matchingMode or non-merchant org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (Cashier / Viewer / insufficient role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/fulfillment-policy:
    get:
      tags: [orgs]
      operationId: getFulfillmentPolicySettings
      summary: Get merchant fulfillment policy (default on_completed when unset)
      description: >
        When staff may release goods vs chain-final Completed. Merchant Owner,
        Administrator, or Viewer may read. Cashier receives 403. Stamped onto
        each payment order at create; changing default does not rewrite open orders.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Current fulfillment policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FulfillmentPolicySettings"
        "400":
          description: Org is not a merchant (or merchant site)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: putFulfillmentPolicySettings
      summary: Set merchant fulfillment policy (Owner / Administrator only)
      description: >
        on_completed = release goods only after Completed. on_verifying = staff
        may release when tx is detected (Verifying). Does not lower blockchain
        confirmation requirements. Cashier receives 403.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateFulfillmentPolicyRequest"
      responses:
        "200":
          description: Fulfillment policy saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FulfillmentPolicySettings"
        "400":
          description: Invalid fulfillmentPolicy or non-merchant org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (Cashier / Viewer / insufficient role)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/settlement:
    get:
      tags: [orgs]
      operationId: listSettlementAddresses
      summary: List merchant settlement addresses (per asset/network)
      description: >
        Merchant Owner, Administrator, or Viewer (and platform operators / agent
        ancestors) may read. Cashier receives 403. Only merchant and merchant_site
        orgs are valid. Addresses are merchant-controlled receive destinations used
        by matching assignOnCreate — CryptoGate never holds spend keys.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Settlement address book
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementAddressList"
        "400":
          description: Org is not a merchant (or merchant site)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: putSettlementAddress
      summary: Upsert settlement address for an asset/network pair
      description: >
        Owner or Administrator only (Cashier / Viewer 403). Asset+network must be
        enabled in packages/domain registry (Phase 1: USDT + tron). Upserts one
        address per (org, asset, network). Body requires mfaCode.

        First address for a pair activates immediately. Changing an existing address
        writes pendingAddress / pendingActivatesAt (default 24h, SETTLEMENT_COOLDOWN_MS)
        and keeps address as the currently active receive destination until then.
        New payment orders keep using address until cool-down ends. Audit on PUT.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertSettlementAddressRequest"
      responses:
        "200":
          description: Settlement address saved (active or pending_cool_down)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SettlementAddress"
        "400":
          description: Invalid body or non-merchant org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated or invalid MFA code
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (Cashier / Viewer / MFA not enrolled)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Asset/network not enabled in domain registry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/xpub:
    get:
      tags: [orgs]
      operationId: listXpubSettings
      summary: List xPub registration presence (never the full xPub)
      description: >
        Merchant Owner, Administrator, or Viewer (and platform / agent ancestors)
        may read. Cashier 403. Response is presence + cool-down status only —
        never the xPub string, seed, or private key. Mode S HD assign needs an
        active (non-pending) xPub for the asset/network.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: xPub presence list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/XpubSettingsList"
        "400":
          description: Org is not a merchant (or merchant site)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: putXpubSettings
      summary: Register or rotate watch-only xPub (MFA + cool-down)
      description: >
        Owner or Administrator only. Body requires asset, network, xPub, mfaCode.
        First xPub for a pair activates immediately. Rotation stores pendingXPub
        (GET never echoes the string) until pendingActivatesAt. Same cool-down
        default as settlement (24h / SETTLEMENT_COOLDOWN_MS). Audit on PUT.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertXpubRequest"
      responses:
        "200":
          description: xPub presence saved (active or pending_cool_down)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/XpubSettings"
        "400":
          description: Invalid body or non-merchant org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated or invalid MFA code
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (Cashier / Viewer / MFA not enrolled)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Asset/network not enabled in domain registry
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/hd-pool:
    get:
      tags: [orgs]
      operationId: listHdPoolAddresses
      summary: List Mode S HD pool addresses (watch-only)
      description: >
        Same visibility bar as xPub GET (Owner/Admin/Viewer; Cashier 403). Returns
        merchant-controlled receive addresses with FREE / IN_USE / COOLDOWN status.
        Never returns xPub, derivation private keys, or seeds. Claiming a FREE slot
        or deriving a new index happens only inside POST /orders Mode S assign —
        there is no public claim endpoint.

        derivationPath is the relative template under the account xPub (e.g. 0/{index}).
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: HD pool snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HdPoolList"
        "400":
          description: Org is not a merchant (or merchant site)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not in caller subtree
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/setting-overrides:
    get:
      tags: [orgs]
      operationId: listSiteSettingOverrides
      summary: List inherit-override requests for a merchant (site)
      description: >
        Merchant (site) defaults inherit parent merchant settlement, xPub, matching
        mode, and order retention. Site Owner/Administrator request overrides; the
        parent merchant Owner approves or denies. Cashier 403. GET xPub payloads
        never include the full xPub string.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Override history for this site
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettingOverrideList"
        "400":
          description: Org is not a merchant_site
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags: [orgs]
      operationId: requestSiteSettingOverride
      summary: Request a site-level override (site Owner / Administrator)
      description: >
        Creates a pending override. Direct PUT on site settlement / xPub / matching
        mode / retention returns 403 override_required until the parent merchant
        Owner approves. Duplicate pending kind returns 409.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestSiteSettingOverride"
      responses:
        "201":
          description: Pending override created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettingOverride"
        "400":
          description: Invalid settingKind or payload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Pending or already-approved override for this kind
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/setting-overrides/{overrideId}:
    patch:
      tags: [orgs]
      operationId: decideSiteSettingOverride
      summary: Approve or deny a site override (parent merchant Owner)
      description: >
        Approve writes the payload onto the site org (settlement/xPub still require
        mfaCode). Deny leaves inherit in place. Platform compliance uses B7, not this
        path.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
        - name: overrideId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecideSiteSettingOverride"
      responses:
        "200":
          description: Decision recorded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SiteSettingOverride"
        "400":
          description: Invalid decision or missing MFA
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated or invalid MFA
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Not the parent merchant Owner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Pending override not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/notification-preferences:
    get:
      tags: [orgs]
      operationId: getNotificationPreferences
      summary: List notification email/in-app preferences for the caller on this org
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Preference rows (defaults true/true when unset)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationPreferenceList"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: putNotificationPreferences
      summary: Save notification preferences for the caller on this org
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateNotificationPreferencesRequest"
      responses:
        "200":
          description: Saved preferences
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NotificationPreferenceList"
        "400":
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/retention:

    get:
      tags: [orgs]
      operationId: getOrgRetentionSettings
      summary: Get order-delete retention (sites inherit parent until override)
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Effective retention
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgRetentionSettings"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: putOrgRetentionSettings
      summary: Set order-delete retention (Owner / Administrator; site needs override)
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateOrgRetentionRequest"
      responses:
        "200":
          description: Retention saved
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrgRetentionSettings"
        "403":
          description: Forbidden or override_required on a site without approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orders:
    post:
      tags: [orders]
      operationId: createPaymentOrder
      summary: Create a payment order (idempotent with Idempotency-Key)
      description: >
        Merchant or Cashier creates a collection order. Matching mode is the merchant
        default at create time and is stored on the order; the request body must not
        include matchingMode. Payable amount, receive address, addressSource, hdIndex,
        and memoOrTag come from packages/matching assignOnCreate. Mode S same-amount
        conflicts claim or derive from the HD pool under the create lock.

        Mode B (Standard): a second open order with the same payable amount on the
        main settlement address is rejected with 409 `mode_b_amount_in_use` (details
        include the blocking order). Staff must finish/cancel the first order, change
        amount, or Owner switches to Amount fingerprint / Smart address. Mode D:
        assigned memo already held by an open order → 409 `mode_d_memo_in_use`.
        Match-time same-amount / memo collision → Payment Anomaly remains as a safety net.

        Agent-account users receive 403. Cashiers may create orders but cannot set
        matching mode, settlement address, xPub, or fees (those endpoints are 403).
        Volume fee is not deducted from the on-chain payable amount.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/idempotencyKey"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreatePaymentOrderRequest"
      responses:
        "201":
          description: Order created; matching fields locked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentOrder"
        "400":
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Agent cannot create; or Cashier attempted a privileged field
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: >
            Idempotency-Key reused with a different body, matching exhausted,
            or Mode B same-amount create lock (`mode_b_amount_in_use` with
            `details.blockingOrder`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Asset/network not enabled or matching assign rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limit (API key 120/min or IP 300/min)
          headers:
            Retry-After:
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    get:
      tags: [orders]
      operationId: listPaymentOrders
      summary: List or export orders in the caller’s merchant scope
      description: >
        JSON default `{ items }`. `format=csv` (or Accept: text/csv) returns a
        spreadsheet-safe CSV. Cashier JSON is own orders only; Cashier CSV is 403.
        Merchant Owner/Admin/Viewer may list descendant merchant_site orders.
        Query orgId must be inside that tree. Platform operator lists all.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - name: format
          in: query
          schema:
            type: string
            enum: [json, csv]
          description: Default json. csv max 5000 rows; json max 200 (default 100).
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/OrderStatus"
        - name: orgId
          in: query
          schema:
            type: string
            format: uuid
          description: Restrict to one org in the caller’s merchant tree
        - name: agentOrgId
          in: query
          schema:
            type: string
            format: uuid
          description: Restrict to merchant orgs in the agent subtree (platform/agent portals)
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      responses:
        "200":
          description: Order list (JSON) or CSV download
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentOrderList"
            text/csv:
              schema:
                type: string
              description: >
                Headers include matching_mode, payable_amount, receive_address,
                address_source, hd_index, memo_or_tag. Cells neutralize leading =+-@.
        "400":
          description: Invalid format, status, orgId, or limit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Outside merchant scope, or Cashier attempted CSV export
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orders/{id}:
    get:
      tags: [orders]
      operationId: getPaymentOrder
      summary: Order status and received amount (merchant / cashier session)
      description: >
        Returns the payment order including matching fields locked at create.
        Cross-merchant access is 403. Payers must use GET /orders/{id}/payment
        (public) instead of this path.
      parameters:
        - $ref: "#/components/parameters/orderId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      security:
        - sessionCookie: []
        - apiKey: []
      responses:
        "200":
          description: Order status and received amount
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentOrder"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Outside merchant scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orders/{id}/cancel:
    post:
      tags: [orders]
      operationId: cancelPaymentOrder
      summary: Cancel a pending payment order
      description: >
        Sets status to `cancelled` when the order is still `pending_payment`.
        Owner/Administrator may cancel any pending order on their merchant org;
        Cashier may cancel only their own. Verifying/completed orders cannot be
        cancelled (chain may already show a tx). Frees Mode B amount / Mode D
        memo create locks so another cashier can Continue.
      parameters:
        - $ref: "#/components/parameters/orderId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      security:
        - sessionCookie: []
        - apiKey: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                note:
                  type: string
                  maxLength: 500
                  description: Optional audit note
      responses:
        "200":
          description: Order cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentOrder"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Outside scope, or cashier cancelling another user's order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Order is no longer pending
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orders/{id}/resolve-anomaly:
    post:
      tags: [orders]
      operationId: resolvePaymentAnomaly
      summary: Resolve a payment anomaly after manual reconcile
      description: >
        Closes a `payment_anomaly` order after staff reconcile. Requires a note.
        Sets status to `cancelled` while preserving `anomalyReason` for invoice/audit;
        stores `anomalyResolutionNote` and `anomalyResolvedAt`. Never marks the order
        paid. Owner/Administrator: any anomaly on their merchant org; Cashier: own only.
        Resolved rows leave the anomaly list and urgent alerts (no longer `payment_anomaly`).
      parameters:
        - $ref: "#/components/parameters/orderId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      security:
        - sessionCookie: []
        - apiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [note]
              properties:
                note:
                  type: string
                  minLength: 1
                  maxLength: 1000
                  description: >
                    Brief record of what was checked (customer, amount, tx) — required for audit
      responses:
        "200":
          description: Anomaly resolved (order cancelled with resolution note)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentOrder"
        "400":
          description: Missing or invalid note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Outside scope, or cashier resolving another user's order
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Order is no longer a payment anomaly
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orders/{id}/on-chain:
    get:
      tags: [orders]
      operationId: getPaymentOrderOnChain
      summary: On-chain facts for a payment order (watcher-written)
      description: >
        Merchant/cashier session or signed API key. 200 even when no tx yet
        (txHash null). toAddress is the order receive address. Cross-merchant
        403. Guest pay page must not call this — use GET /orders/{id}/payment.
      parameters:
        - $ref: "#/components/parameters/orderId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      security:
        - sessionCookie: []
        - apiKey: []
      responses:
        "200":
          description: Hash, height, addresses, amount, time
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OnChainDetails"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Outside merchant scope
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Order not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orders/{id}/payment:
    get:
      tags: [orders]
      operationId: getPaymentOrderPayment
      summary: Public guest-pay payload (no merchant session)
      description: >
        Polled by apps/payment-page and cashier QR screens. No login.
        Order ids are opaque; response must not include settlement keys, xPub,
        fee rates, or merchant session data. Do not poll the chain from the
        payment page — poll this endpoint for status.

        Expired and anomaly orders still return 200 with status set so the
        guest page can render those states. Unknown id returns 404.
      parameters:
        - $ref: "#/components/parameters/orderId"
      security: []
      responses:
        "200":
          description: Payment page URL, QR payload, copy-ready address and amount
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PaymentDetails"
        "404":
          description: Payment link not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Guest pay poll rate limit (60/min per IP)
          headers:
            Retry-After:
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api-keys:
    get:
      tags: [api-keys]
      operationId: listApiKeys
      summary: List API keys for the caller merchant org (never secret)
      description: >
        Owner/Admin/Viewer. Cashier 403. Agent accounts cannot list merchant keys.
        Returns active keys only (revoked rows omitted). Never returns `secret`.
        Max active keys per merchant org: 10 (`API_KEY_MAX_PER_ORG`).
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      responses:
        "200":
          description: Active key metadata without secrets
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiKeyList"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier / agent)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags: [api-keys]
      operationId: createApiKey
      summary: Create a machine API key; secret shown once
      description: >
        Owner/Admin only. Cashier 403. Agent cannot create merchant keys.
        `secret` is returned once on 201 and never again — store hashed at rest;
        never log. `keyId` is the public id for `X-Api-Key` (prefix `cgk_`).
        HMAC uses `secret` as in v0.3.0. At most 10 active keys per org → 409
        when full. Audit-log create.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateApiKeyRequest"
      responses:
        "201":
          description: Key created; secret shown once
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiKeyCreated"
        "400":
          description: Invalid label or expiresAt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (Cashier / Viewer / agent)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Org already at API_KEY_MAX_PER_ORG active keys
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          headers:
            Retry-After:
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api-keys/{apiKeyId}:
    delete:
      tags: [api-keys]
      operationId: revokeApiKey
      summary: Revoke an API key (soft revoke)
      description: >
        Owner/Admin. Cashier 403. Sets revoked_at; HMAC with that keyId fails
        immediately. Idempotent if already revoked → 204. Audit-log revoke.
        Confirm in UI before calling (dangerous action).
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiKeyId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      responses:
        "204":
          description: Revoked (or already revoked)
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found in caller org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /api-keys/{apiKeyId}/rotate:
    post:
      tags: [api-keys]
      operationId: rotateApiKey
      summary: Revoke key and issue a replacement; secret shown once
      description: >
        Owner/Admin. Cashier 403. Atomically revokes `{apiKeyId}` and creates a
        new active key (same label; optional new expiresAt in body). Returns the
        new key with `secret` once — same bar as create. Old keyId stops working
        immediately. 404 if target missing or already revoked. Does not count
        against max until the old row is revoked (net active count unchanged).
        Audit-log rotate.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiKeyId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RotateApiKeyRequest"
      responses:
        "201":
          description: Replacement key created; secret shown once
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiKeyCreated"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: apiKeyId not active in caller org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          headers:
            Retry-After:
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /webhooks:
    get:
      tags: [webhooks]
      operationId: listWebhooks
      summary: List webhook endpoints (never signingSecret)
      description: >
        Same visibility bar as xPub GET (Owner/Admin/Viewer; Cashier 403).
        GET never returns signingSecret. Max 5 endpoints per merchant org.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      responses:
        "200":
          description: Endpoint list without secrets
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEndpointList"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags: [webhooks]
      operationId: registerWebhook
      summary: Register a merchant callback URL
      description: >
        HTTPS only (http://127.0.0.1 or http://localhost allowed in non-production).
        signingSecret is returned once on 201 and never again. Cashier 403.
        Agent accounts cannot register merchant callbacks. Duplicate URL → 409.
        Merchant fulfillment must use these signed events — not browser redirect.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterWebhookRequest"
      responses:
        "201":
          description: Endpoint created; signingSecret shown once
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookCreated"
        "400":
          description: Invalid URL or events
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (Cashier / agent)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: URL already registered for this org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "429":
          description: Rate limited
          headers:
            Retry-After:
              schema:
                type: integer
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /webhooks/{webhookId}/rotate-secret:
    post:
      tags: [webhooks]
      operationId: rotateWebhookSecret
      summary: Rotate webhook signing secret (shown once)
      description: >
        Owner/Admin only. Replaces signing_secret in place and returns it once.
        GET/list never returns signingSecret.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/webhookId"
      responses:
        "200":
          description: Endpoint metadata with new signingSecret once
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookCreated"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Webhook not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /webhooks/test:

    post:
      tags: [webhooks]
      operationId: testWebhook
      summary: Enqueue a signed webhook.test sample
      description: >
        Delivery worker signs the payload with the endpoint secret (M3-14).
        Returns 202 when queued. Optional body webhookId; otherwise all enabled
        endpoints for the caller’s merchant org.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TestWebhookRequest"
      responses:
        "202":
          description: Signed sample event accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookTestAccepted"
        "401":
          description: Not authenticated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: webhookId not in caller org
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /webhooks/{webhookId}:
    delete:
      tags: [webhooks]
      operationId: deleteWebhook
      summary: Disable and remove a webhook endpoint
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/webhookId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      responses:
        "204":
          description: Removed
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /webhooks/{webhookId}/deliveries:
    get:
      tags: [webhooks]
      operationId: listWebhookDeliveries
      summary: Recent delivery attempts (M3-14 worker)
      description: Newest first. Retries reuse the same eventId.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/webhookId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Delivery log
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookDeliveryList"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /webhooks/{webhookId}/deliveries/{deliveryId}/resend:
    post:
      tags: [webhooks]
      operationId: resendWebhookDelivery
      summary: Resend a webhook delivery
      description: >
        Clones a failed or success delivery as a new pending row with the same
        body and event id. Replay uses a new delivery id. See
        doc/Webhook-Verification.md.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/webhookId"
        - name: deliveryId
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      responses:
        "202":
          description: Resend queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookDelivery"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Delivery not in a resendable state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /commission-payouts:
    get:
      tags: [orgs]
      operationId: listCommissionPayouts
      summary: List commission payout slips
      description: Platform → agent and agent → sub payout history (Decision 1b).
      security:
        - sessionCookie: []
      parameters:
        - name: payer
          in: query
          schema:
            type: string
            enum: [platform, agent]
        - name: payeeOrgId
          in: query
          schema:
            type: string
            format: uuid
        - name: payerOrgId
          in: query
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Payout list
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/CommissionPayout"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags: [orgs]
      operationId: upsertCommissionPayout
      summary: Prepare / upsert a commission payout slip
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpsertCommissionPayoutRequest"
      responses:
        "200":
          description: Upserted slip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommissionPayout"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /commission-payouts/generate:
    post:
      tags: [orgs]
      operationId: generateCommissionInvoices
      summary: Generate month-end platform → agent commission invoices
      description: >
        Creates or refreshes issued invoices for all top-level agents for the
        period (default current UTC month). Fee base = paid volume fees in the
        agent subtree. Skips agents already paid or settled for that period.
      security:
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                periodKey:
                  type: string
                  description: YYYY-MM (defaults to current calendar month UTC)
                  example: "2026-08"
      responses:
        "200":
          description: Generated invoices
          content:
            application/json:
              schema:
                type: object
                required: [periodKey, periodLabel, created, skipped]
                properties:
                  periodKey:
                    type: string
                  periodLabel:
                    type: string
                  created:
                    type: array
                    items:
                      $ref: "#/components/schemas/CommissionPayout"
                  skipped:
                    type: array
                    items:
                      type: object
                      additionalProperties: true
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /agent-commissions:
    get:
      tags: [orgs]
      operationId: listAgentCommissions
      summary: List agent commission percents (batch)
      description: >
        Returns commission settings for all agent/agent_sub orgs visible to the
        caller. Used by platform and agent commission boards to avoid N+1
        per-org GETs. Orgs without a stored row are omitted — clients apply the
        default percent (15).
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Commission list
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/AgentCommissionSettings"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /agent-payout-addresses:
    get:
      tags: [orgs]
      operationId: listAgentPayoutAddresses
      summary: List agent commission payout addresses (batch)
      description: >
        Returns configured payout addresses for visible agent/agent_sub orgs.
        Orgs without an address are omitted (same as per-org 404).
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Payout address list
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/AgentPayoutAddress"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /commission-payouts/{payoutId}/confirm-sent:
    post:
      tags: [orgs]
      operationId: confirmCommissionPayoutSent
      summary: Confirm USDT-TRON remittance sent (ready → verifying)
      security:
        - sessionCookie: []
      parameters:
        - name: payoutId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                note:
                  type: string
                  nullable: true
                  maxLength: 2000
      responses:
        "200":
          description: Slip in verification
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommissionPayout"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /commission-payouts/{payoutId}/mark-paid:
    post:
      tags: [orgs]
      operationId: markCommissionPayoutPaid
      summary: Mark remittance paid (platform invoice or agent→sub slip)
      description: >
        Platform → agent issued → paid (awaiting agent confirm).
        Agent → sub ready/verifying → paid. Note required without txRef from issued/ready.
      security:
        - sessionCookie: []
      parameters:
        - name: payoutId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                txRef:
                  type: string
                  nullable: true
                  description: >
                    Optional chain tx hash. Watcher may set this later for USDT-TRON.
                note:
                  type: string
                  nullable: true
                  maxLength: 2000
                  description: Required when completing from issued/ready without a tx.
      responses:
        "200":
          description: Updated slip
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommissionPayout"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /commission-payouts/{payoutId}/agent-confirm:
    post:
      tags: [orgs]
      operationId: agentConfirmCommissionPayout
      summary: Agent confirms receipt (paid → settled)
      description: >
        Agent Owner/Admin acknowledges the platform remittance. Settled invoices
        appear in platform Payout history.
      security:
        - sessionCookie: []
      parameters:
        - name: payoutId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Settled invoice
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CommissionPayout"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "409":
          description: Invalid state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /service-bills:
    get:
      tags: [service-bills]
      operationId: listServiceBills
      summary: List service bills (not payment orders)
      description: >
        Separate rail from /orders. USD subscription + volume fee. Cashier 403.
        Merchant Owner/Admin/Viewer — own org. Agent — subtree read-only.
        Platform Owner/Admin — any org via query orgId.
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
        - name: orgId
          in: query
          schema:
            type: string
            format: uuid
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ServiceBillStatus"
      responses:
        "200":
          description: Service bill list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceBillList"
        "403":
          description: Forbidden (e.g. Cashier)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    post:
      tags: [service-bills]
      operationId: issueServiceBill
      summary: Issue a service bill (platform only)
      description: >
        Platform Owner/Administrator only. Does not create a payment order and
        must not debit merchant settlement or payer on-chain amounts.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IssueServiceBillRequest"
      responses:
        "201":
          description: Bill issued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceBill"
        "403":
          description: Not a platform operator
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /service-bills/generate:
    post:
      tags: [service-bills]
      operationId: generateServiceBills
      summary: Generate monthly service bills from confirmed volume
      description: >
        Platform Owner/Administrator only. Issues one USD service bill per
        active merchant for the period (subscription + volume fee on completed
        payment-order volume). Does not debit payer on-chain amounts. Idempotent
        per org + period (existing non-voided bills are skipped).
      security:
        - sessionCookie: []
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/GenerateServiceBillsRequest"
      responses:
        "200":
          description: Generation result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GenerateServiceBillsResult"
        "403":
          description: Not a platform operator
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /service-bills/{billId}:
    get:
      tags: [service-bills]
      operationId: getServiceBill
      summary: Service bill detail
      security:
        - sessionCookie: []
        - apiKey: []
      parameters:
        - $ref: "#/components/parameters/billId"
        - $ref: "#/components/parameters/apiTimestamp"
        - $ref: "#/components/parameters/apiNonce"
        - $ref: "#/components/parameters/apiSignature"
      responses:
        "200":
          description: Service bill
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceBill"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    patch:
      tags: [service-bills]
      operationId: updateServiceBill
      summary: Mark paid, void, or adjust a service bill (platform only)
      description: >
        Platform Owner/Administrator only. mark_paid: issued or overdue → paid.
        void: issued → voided (reason required). adjust: issued or overdue —
        delta on totalAmount (reason required). Cashier 403.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/billId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateServiceBillRequest"
      responses:
        "200":
          description: Updated bill
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceBill"
        "403":
          description: Not a platform operator
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Invalid transition
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /service-bills/{billId}/checkout:
    get:
      tags: [service-bills]
      operationId: getServiceBillCheckout
      summary: Platform-billing checkout payload (not guest /orders/payment)
      description: >
        Merchant Owner/Admin session. QR/link pay the platform billing wallet.
        Must not reuse PaymentDetails or payment-page routes.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/billId"
      responses:
        "200":
          description: Checkout instructions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceBillCheckout"
        "403":
          description: Forbidden (Cashier / agent)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /audit:
    get:
      tags: [audit]
      operationId: listAuditLog
      summary: List append-only audit events (read-only)
      description: >
        Platform operators: global filters. Agent and merchant roles: events for
        orgs in authorized subtree only. Cashier 403. No DELETE — immutable log.
      security:
        - sessionCookie: []
      parameters:
        - name: from
          in: query
          schema:
            type: string
            format: date-time
        - name: to
          in: query
          schema:
            type: string
            format: date-time
        - name: actorUserId
          in: query
          schema:
            type: string
            format: uuid
        - name: orgId
          in: query
          schema:
            type: string
            format: uuid
        - name: action
          in: query
          schema:
            $ref: "#/components/schemas/AuditAction"
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 500
            default: 100
      responses:
        "200":
          description: Audit entries (newest first)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuditLogList"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/settings/fee-tiers:
    get:
      tags: [platform-settings]
      operationId: getFeeTierSettings
      summary: Read global Small/Mid/Enterprise tier bands
      description: >
        Platform, agent, and merchant Owner/Administrator/Viewer may read bands
        for onboard sliders and effective-rate display. Cashier 403. Volume fee is
        never deducted from payer on-chain amounts.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Global tier configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformFeeTierSettings"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [platform-settings]
      operationId: updateFeeTierSettings
      summary: Update global tier bands (Platform Owner only)
      description: >
        Changes apply to the **next billing period** only — not retroactive.
        Platform Administrator may read but not edit. Logged to audit.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePlatformFeeTierSettingsRequest"
      responses:
        "200":
          description: Updated tier configuration
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformFeeTierSettings"
        "403":
          description: Not Platform Owner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/settings/org-policy:
    get:
      tags: [platform-settings]
      operationId: getPlatformOrgPolicy
      summary: Read platform org policy (max agent depth)
      description: Platform Owner/Administrator/Viewer read. Agent and merchant 403.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Org policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformOrgPolicy"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [platform-settings]
      operationId: updatePlatformOrgPolicy
      summary: Update max agent nesting depth (Platform Owner only)
      description: >
        Phase 1 default **2** (agent → agent (sub) → merchant). Changing depth
        requires confirmation in UI; logged to audit with warning metadata.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePlatformOrgPolicyRequest"
      responses:
        "200":
          description: Updated policy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformOrgPolicy"
        "403":
          description: Not Platform Owner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/settings/billing-wallet:
    get:
      tags: [platform-settings]
      operationId: getBillingWalletSettings
      summary: Read platform billing wallet / invoice seller identity (B11-lite)
      description: >
        Platform Owner/Administrator/Viewer. Remittance `payTo` for service-bill
        checkout and invoice face. Not merchant settlement.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Billing wallet settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformBillingWalletSettings"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [platform-settings]
      operationId: updateBillingWalletSettings
      summary: Update billing wallet / seller identity (Platform Owner only)
      description: >
        Updates invoice seller name/email and service-bill remittance destination.
        Logged as billing_wallet_put. Does not affect merchant settlement addresses.
      security:
        - sessionCookie: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdatePlatformBillingWalletSettingsRequest"
      responses:
        "200":
          description: Updated billing wallet settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformBillingWalletSettings"
        "403":
          description: Not Platform Owner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/commercial:
    get:
      tags: [orgs]
      operationId: getMerchantCommercial
      summary: Merchant commercial settings (tier + volume fee %)
      description: >
        Merchant org accounts only. Platform and agent roles may read merchants in
        authorized subtree. Merchant Owner/Administrator/Viewer read own org.
        Cashier 403.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Commercial settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantCommercialSettings"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or not a merchant account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: updateMerchantCommercial
      summary: Assign tier and volume fee within platform band
      description: >
        Agent Owner/Administrator for merchants in subtree; Platform
        Owner/Administrator for any merchant. Rate must fall inside the tier band
        unless tier is Enterprise. Changes apply **immediately** to the merchant's
        effective rate. Enterprise rates outside the band create a pending approval
        (see /platform/enterprise-rate-approvals). Merchant roles and Cashier 403.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateMerchantCommercialRequest"
      responses:
        "200":
          description: Updated or pending approval
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MerchantCommercialSettings"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Rate outside band or invalid tier
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/agent-payout:
    get:
      tags: [orgs]
      operationId: getAgentPayoutAddress
      summary: Agent commission payout address
      description: >
        Agent and agent_sub orgs only. Platform staff and agent org
        Owner/Administrator/Viewer may read. Returns 404 when not configured.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Payout address
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentPayoutAddress"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found or payout not configured
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: updateAgentPayoutAddress
      summary: Set agent commission payout address
      description: >
        Agent org Owner/Administrator only. Watch-only destination for monthly
        commission payouts — not used for payer on-chain flow.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAgentPayoutRequest"
      responses:
        "200":
          description: Updated payout address
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentPayoutAddress"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Invalid asset/network
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /orgs/{orgId}/agent-commission:
    get:
      tags: [orgs]
      operationId: getAgentCommission
      summary: Agent commission percent of platform fee
      description: >
        Agent and agent_sub orgs only. Platform staff and agent org
        Owner/Administrator/Viewer may read. Defaults to 15% when unset.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Commission settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentCommissionSettings"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Org not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
    put:
      tags: [orgs]
      operationId: updateAgentCommission
      summary: Set agent commission percent (applies immediately)
      description: >
        Platform Owner/Administrator only. Updates take effect immediately for
        new commission accruals — not taken from payer on-chain payments.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateAgentCommissionRequest"
      responses:
        "200":
          description: Updated commission
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentCommissionSettings"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "422":
          description: Invalid percent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /org-member-emails:
    get:
      tags: [orgs]
      operationId: listOrgMemberEmails
      summary: Bulk member emails for visible orgs (list search and invite validation)
      description: >
        Returns member emails grouped by org in one query. Platform staff see all
        orgs; agent/merchant Owner/Administrator see their authorized subtree.
        Aliases: /platform/org-member-emails, /platform/org-emails.
      security:
        - sessionCookie: []
      parameters:
        - name: types
          in: query
          description: Comma-separated org_accounts.type values to include
          schema:
            type: string
      responses:
        "200":
          description: Org member email index
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformOrgMemberEmailList"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/org-emails:
    get:
      tags: [platform-settings]
      operationId: listPlatformOrgEmails
      summary: Alias for listPlatformOrgMemberEmails (bulk org member emails)
      security:
        - sessionCookie: []
      parameters:
        - name: types
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Org member email index
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformOrgMemberEmailList"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/org-member-emails:
    get:
      tags: [platform-settings]
      operationId: listPlatformOrgMemberEmails
      summary: Bulk member emails for visible orgs (platform list search)
      description: >
        Returns member emails grouped by org in one query. Platform Owner,
        Administrator, or Viewer. Replaces N calls to listOrgUsers when
        searching agents or merchants by contact email.
      security:
        - sessionCookie: []
      parameters:
        - name: types
          in: query
          description: Comma-separated org_accounts.type values to include (e.g. agent,agent_sub)
          schema:
            type: string
      responses:
        "200":
          description: Org member email index
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlatformOrgMemberEmailList"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/enterprise-rate-approvals:
    get:
      tags: [platform-settings]
      operationId: listEnterpriseRateApprovals
      summary: List pending Enterprise custom rate requests
      description: Platform Owner/Administrator/Viewer. Cashier 403.
      security:
        - sessionCookie: []
      parameters:
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/EnterpriseRateApprovalStatus"
        - name: limit
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 50
      responses:
        "200":
          description: Approval queue
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnterpriseRateApprovalList"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/enterprise-rate-approvals/{approvalId}:
    patch:
      tags: [platform-settings]
      operationId: decideEnterpriseRateApproval
      summary: Approve or deny Enterprise custom rate (Platform Owner only)
      security:
        - sessionCookie: []
      parameters:
        - name: approvalId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DecideEnterpriseRateApprovalRequest"
      responses:
        "200":
          description: Decision recorded; merchant commercial updated on approve
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EnterpriseRateApproval"
        "403":
          description: Not Platform Owner
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/watcher-health:
    get:
      tags: [platform-settings]
      operationId: getWatcherHealth
      summary: Latest per-network watcher heartbeats
      description: >
        Platform Owner/Administrator/Viewer. Returns last tick summary written by
        apps/watcher (RPC mode, ingest mode, lag-adjusted health score). Empty
        items when the watcher has not yet persisted a heartbeat.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Watcher health snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WatcherHealthList"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/networks/catalog:
    get:
      tags: [platform-settings]
      operationId: getNetworkCatalog
      summary: Network & asset catalog (B16)
      description: >
        Platform Owner/Administrator/Viewer. One card per network from the
        domain registry, merged with persisted maintenance and watcher heartbeats.
        Catalog fraction is enabled pairs / catalogued pairs — not a fake health %.
        Ingest healthScore comes from watcher heartbeats when present.
        Each card/pair includes an orderability lamp (Open / Paused / Down / Off).
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Network catalog snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkCatalog"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /networks/status:
    get:
      tags: [platform-settings]
      operationId: getNetworksStatus
      summary: Orderability lamps for all networks (any authenticated portal)
      description: >
        Any authenticated session (platform, agent, merchant). Compact Open /
        Paused / Down / Off lamps from registry enablement + maintenance +
        watcher ingest. Used by merchant Networks and System Health tables.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Network status snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworksStatus"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/networks/{network}/maintenance:
    put:
      tags: [platform-settings]
      operationId: putNetworkMaintenance
      summary: Enable or clear network maintenance (deposit pause)
      description: >
        Platform Owner or Administrator. While active, POST /orders for that
        network returns 422 network_maintenance. Merchants see the message via
        GET /network-maintenance. Audited.
      security:
        - sessionCookie: []
      parameters:
        - name: network
          in: path
          required: true
          schema:
            $ref: "#/components/schemas/NetworkId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NetworkMaintenanceUpdate"
      responses:
        "200":
          description: Updated maintenance window
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NetworkMaintenanceState"
        "400":
          description: Invalid body
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Unknown network
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /network-maintenance:
    get:
      tags: [platform-settings]
      operationId: listActiveNetworkMaintenance
      summary: Active network maintenance windows (all authenticated portals)
      description: >
        Any authenticated session. Used for merchant/agent deposit-pause banners.
        Returns only networks currently under effective maintenance.
      security:
        - sessionCookie: []
      responses:
        "200":
          description: Active maintenance list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActiveNetworkMaintenanceList"
        "401":
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/orgs/{orgId}/compliance-overrides:
    get:
      tags: [platform-settings]
      operationId: listComplianceOverrides
      summary: List compliance overrides for a merchant (B7 log)
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      responses:
        "200":
          description: Override history (newest first)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComplianceOverrideList"
        "403":
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Merchant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
  /platform/orgs/{orgId}/compliance-override:
    post:
      tags: [platform-settings]
      operationId: applyComplianceOverride
      summary: Apply B7 compliance override (MFA + immutable audit)
      description: >
        Platform Owner or Administrator. Requires enrolled MFA. Override types:
        settlement_address (immediate), matching_mode, suspend_order_create,
        suspend_merchant (pauses the org). Writes compliance_overrides and audit_log.
      security:
        - sessionCookie: []
      parameters:
        - $ref: "#/components/parameters/orgId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ComplianceOverrideRequest"
      responses:
        "200":
          description: Override applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ComplianceOverrideResult"
        "401":
          description: Invalid MFA code
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "403":
          description: Forbidden or MFA not enrolled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "404":
          description: Merchant not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: cg_session
    apiKey:
      type: apiKey
      in: header
      name: X-Api-Key
      description: >
        Machine client id. Must be paired with X-Timestamp, X-Nonce, X-Signature
        (see info description and M3-01). Session cookie callers omit all four.
  parameters:
    orderId:
      name: id
      in: path
      required: true
      schema:
        type: string
    orgId:
      name: orgId
      in: path
      required: true
      schema:
        type: string
    userId:
      name: userId
      in: path
      required: true
      schema:
        type: string
    idempotencyKey:
      name: Idempotency-Key
      in: header
      required: true
      description: >
        Required on POST /orders. Reuse of the same key with the same body
        returns the original 201 body. Same key with a different body is 409.
      schema:
        type: string
        minLength: 8
        maxLength: 128
    apiTimestamp:
      name: X-Timestamp
      in: header
      required: false
      description: >
        Unix seconds (UTC). Required when using X-Api-Key. Rejected when
        |now − value| > 300 (API_SIGNING_MAX_SKEW_SECONDS). Omit with session cookie.
      schema:
        type: string
        pattern: "^[0-9]{10,12}$"
    apiNonce:
      name: X-Nonce
      in: header
      required: false
      description: >
        16–64 chars [A-Za-z0-9_-]. Unique per API key for 600s
        (API_SIGNING_NONCE_TTL_SECONDS). Reuse → 401 nonce_replay.
      schema:
        type: string
        minLength: 16
        maxLength: 64
        pattern: "^[A-Za-z0-9_-]{16,64}$"
    apiSignature:
      name: X-Signature
      in: header
      required: false
      description: >
        Lowercase hex HMAC-SHA256(apiSecret, canonical). Canonical =
        timestamp LF nonce LF METHOD LF pathAndQuery LF sha256Hex(rawBody).
      schema:
        type: string
        pattern: "^[0-9a-f]{64}$"
    webhookId:
      name: webhookId
      in: path
      required: true
      schema:
        type: string
        format: uuid
    apiKeyId:
      name: apiKeyId
      in: path
      required: true
      description: Internal UUID of the api_keys row (not the public keyId / X-Api-Key)
      schema:
        type: string
        format: uuid
    billId:
      name: billId
      in: path
      required: true
      schema:
        type: string
        format: uuid
  schemas:
    OrgType:
      type: string
      enum: [platform, agent, agent_sub, merchant, merchant_site]
    UserRole:
      type: string
      enum: [owner, administrator, viewer, cashier]
    MembershipStatus:
      type: string
      description: Matches @cryptogate/domain MembershipStatus
      enum: [active, paused]
    OrderStatus:
      type: string
      enum:
        - pending_payment
        - verifying
        - confirmed
        - completed
        - expired
        - payment_anomaly
        - failed
        - cancelled
    MatchingMode:
      type: string
      description: B Standard, C Amount fingerprint, D Memo tag, S Smart address
      enum: [B, C, D, S]
    FulfillmentPolicy:
      type: string
      description: When merchant staff may release goods (chain status unchanged)
      enum: [on_completed, on_verifying]
    MatchingModeSettings:
      type: object
      required: [orgId, matchingMode]
      properties:
        orgId:
          type: string
          description: Merchant or merchant_site org id (the requested org)
        matchingMode:
          $ref: "#/components/schemas/MatchingMode"
        underpayTolerance:
          type: string
          description: >
            Non-negative major-unit decimal string. Mode B underpay allowance;
            same-amount collisions still become Payment Anomaly.
          example: "0"
        source:
          $ref: "#/components/schemas/SettingsSource"
        parentOrgId:
          type: string
          format: uuid
          nullable: true
        effectiveOrgId:
          type: string
          format: uuid
          description: Org whose stored matching mode was returned
    FulfillmentPolicySettings:
      type: object
      required: [orgId, fulfillmentPolicy]
      properties:
        orgId:
          type: string
          description: Merchant or merchant_site org id (the requested org)
        fulfillmentPolicy:
          $ref: "#/components/schemas/FulfillmentPolicy"
        source:
          $ref: "#/components/schemas/SettingsSource"
        parentOrgId:
          type: string
          format: uuid
          nullable: true
        effectiveOrgId:
          type: string
          format: uuid
          description: Org whose stored fulfillment policy was returned
    UpdateFulfillmentPolicyRequest:
      type: object
      required: [fulfillmentPolicy]
      additionalProperties: false
      properties:
        fulfillmentPolicy:
          $ref: "#/components/schemas/FulfillmentPolicy"
    SettingsSource:
      type: string
      description: >
        merchant = parent merchant account. inherit = site using parent defaults.
        override = site after parent Owner approval.
      enum: [merchant, inherit, override]
    SiteOverrideKind:
      type: string
      enum: [settlement, xpub, matching_mode, order_retention, fulfillment_policy]
    SiteOverrideStatus:
      type: string
      enum: [pending, approved, denied]
    SiteSettingOverride:
      type: object
      required: [id, siteOrgId, parentOrgId, settingKind, status, payload, createdAt]
      properties:
        id:
          type: string
          format: uuid
        siteOrgId:
          type: string
          format: uuid
        parentOrgId:
          type: string
          format: uuid
        settingKind:
          $ref: "#/components/schemas/SiteOverrideKind"
        status:
          $ref: "#/components/schemas/SiteOverrideStatus"
        payload:
          type: object
          additionalProperties: true
          description: Proposed values; xPub GET shape uses xPubConfigured only
        requestedBy:
          type: string
          format: uuid
        decidedBy:
          type: string
          format: uuid
          nullable: true
        decidedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
    SiteSettingOverrideList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SiteSettingOverride"
    RequestSiteSettingOverride:
      type: object
      required: [settingKind, payload]
      properties:
        settingKind:
          $ref: "#/components/schemas/SiteOverrideKind"
        payload:
          type: object
          additionalProperties: true
    DecideSiteSettingOverride:
      type: object
      required: [decision]
      properties:
        decision:
          type: string
          enum: [approve, deny]
        reason:
          type: string
          description: Required when denying
        mfaCode:
          type: string
          minLength: 6
          maxLength: 8
          description: Required when approving settlement or xPub
    OrgRetentionSettings:
      type: object
      required: [orgId, orderDeleteDays]
      properties:
        orgId:
          type: string
          format: uuid
        orderDeleteDays:
          type: integer
          minimum: 7
          maximum: 3650
        source:
          $ref: "#/components/schemas/SettingsSource"
        parentOrgId:
          type: string
          format: uuid
          nullable: true
        effectiveOrgId:
          type: string
          format: uuid
    UpdateOrgRetentionRequest:
      type: object
      required: [orderDeleteDays]
      properties:
        orderDeleteDays:
          type: integer
          minimum: 7
          maximum: 3650
    UpdateMatchingModeRequest:
      type: object
      required: [matchingMode]
      properties:
        matchingMode:
          $ref: "#/components/schemas/MatchingMode"
        underpayTolerance:
          type: string
          description: Non-negative major-unit decimal string (Mode B)
    SettlementAddress:
      type: object
      required: [orgId, asset, network, address, status]
      properties:
        orgId:
          type: string
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        address:
          type: string
          description: Currently active receive address (orders still use this during cool-down)
          example: TX7s39gK1p9ZqR5mY8bV2wXn5uH4kP9qR2
        pendingAddress:
          type: string
          nullable: true
          description: Next address; ignored by assignOnCreate until pendingActivatesAt
        pendingActivatesAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [active, pending_cool_down]
    SettlementAddressList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/SettlementAddress"
        source:
          $ref: "#/components/schemas/SettingsSource"
        parentOrgId:
          type: string
          format: uuid
          nullable: true
        effectiveOrgId:
          type: string
          format: uuid
    AgentPayoutAddress:
      type: object
      required: [orgId, asset, network, address]
      properties:
        orgId:
          type: string
          format: uuid
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        address:
          type: string
          description: Watch-only commission payout destination (active)
        pendingAddress:
          type: string
          nullable: true
          description: Next address; ignored until pendingActivatesAt
        pendingActivatesAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
    UpdateAgentPayoutRequest:
      type: object
      required: [asset, network, address, mfaCode]
      properties:
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        address:
          type: string
          minLength: 1
          description: No whitespace
        mfaCode:
          type: string
          minLength: 6
          maxLength: 8
          description: Required to change agent payout address
    AgentCommissionSettings:
      type: object
      required: [orgId, commissionPercent, effectiveFrom]
      properties:
        orgId:
          type: string
          format: uuid
        commissionPercent:
          type: string
          description: Percent of platform fee paid to the agent (rebate)
          example: "15"
        effectiveFrom:
          type: string
          format: date
        updatedAt:
          type: string
          format: date-time
    UpdateAgentCommissionRequest:
      type: object
      required: [commissionPercent]
      properties:
        commissionPercent:
          type: string
          description: Applies immediately (0–100)
          example: "18"
    CommissionPayout:
      type: object
      required:
        - id
        - payeeOrgId
        - payeeName
        - payer
        - periodKey
        - periodLabel
        - platformFeeCollected
        - commissionPercent
        - commissionAmount
        - payoutStatus
        - paymentLink
      properties:
        id:
          type: string
        payeeOrgId:
          type: string
          format: uuid
        payeeName:
          type: string
        payer:
          type: string
          enum: [platform, agent]
        payerOrgId:
          type: string
          format: uuid
          nullable: true
        periodKey:
          type: string
        periodLabel:
          type: string
        platformFeeCollected:
          type: number
        commissionPercent:
          type: string
        commissionAmount:
          type: number
        payoutStatus:
          type: string
          enum: [issued, ready, verifying, paid, settled]
          description: >
            Platform → agent: issued → paid (ops remitted) → settled (agent confirm).
            Agent → sub: ready → verifying → paid.
        payoutAddress:
          type: string
          nullable: true
        asset:
          type: string
          nullable: true
        network:
          type: string
          nullable: true
        paymentLink:
          type: string
        txRef:
          type: string
          nullable: true
        note:
          type: string
          nullable: true
          description: Optional ops note when confirming remittance paid
        treeSnapshot:
          type: object
          nullable: true
          description: Merchant/site fee status + onboard dates for the invoice period
          additionalProperties: true
        paidAt:
          type: string
          format: date-time
          nullable: true
        settledAt:
          type: string
          format: date-time
          nullable: true
        agentConfirmedBy:
          type: string
          format: uuid
          nullable: true
        updatedAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
    UpsertCommissionPayoutRequest:
      type: object
      required:
        - payeeOrgId
        - payeeName
        - payer
        - periodKey
        - periodLabel
        - platformFeeCollected
        - commissionPercent
        - commissionAmount
        - paymentLink
      properties:
        payeeOrgId:
          type: string
          format: uuid
        payeeName:
          type: string
        payer:
          type: string
          enum: [platform, agent]
        payerOrgId:
          type: string
          format: uuid
          nullable: true
        periodKey:
          type: string
        periodLabel:
          type: string
        platformFeeCollected:
          type: number
        commissionPercent:
          type: string
        commissionAmount:
          type: number
        payoutAddress:
          type: string
          nullable: true
        asset:
          type: string
          nullable: true
        network:
          type: string
          nullable: true
        paymentLink:
          type: string
    UpsertSettlementAddressRequest:
      type: object
      required: [asset, network, address, mfaCode]
      properties:
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        address:
          type: string
          minLength: 1
          description: No whitespace
        mfaCode:
          type: string
          minLength: 6
          maxLength: 8
          description: TOTP; required on every settlement PUT
    XpubSettings:
      type: object
      required: [orgId, asset, network, xPubConfigured, pendingXPub, status]
      description: Presence only — never the xPub string
      properties:
        orgId:
          type: string
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        xPubConfigured:
          type: boolean
        pendingXPub:
          type: boolean
          description: True when a rotation is waiting on cool-down (string not returned)
        pendingActivatesAt:
          type: string
          format: date-time
          nullable: true
        status:
          type: string
          enum: [active, pending_cool_down]
    XpubSettingsList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/XpubSettings"
        source:
          $ref: "#/components/schemas/SettingsSource"
        parentOrgId:
          type: string
          format: uuid
          nullable: true
        effectiveOrgId:
          type: string
          format: uuid
    UpsertXpubRequest:
      type: object
      required: [asset, network, xPub, mfaCode]
      properties:
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        xPub:
          type: string
          minLength: 20
          description: Watch-only extended public key; write-only (never echoed on GET)
        mfaCode:
          type: string
          minLength: 6
          maxLength: 8
    HdPoolState:
      type: string
      description: Mode S pool slot state (matches @cryptogate/domain HdPoolState)
      enum: [FREE, IN_USE, COOLDOWN]
    HdPoolAddress:
      type: object
      required: [id, orgId, asset, network, hdIndex, receiveAddress, status]
      description: Watch-only derived receive address — never xPub
      properties:
        id:
          type: string
          format: uuid
        orgId:
          type: string
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        hdIndex:
          type: integer
          minimum: 0
        receiveAddress:
          type: string
        status:
          $ref: "#/components/schemas/HdPoolState"
        cooldownUntil:
          type: string
          format: date-time
          nullable: true
        lastOrderId:
          type: string
          nullable: true
          description: Last order that held this address (audit)
    HdPoolList:
      type: object
      required: [derivationPath, items]
      properties:
        derivationPath:
          type: string
          description: Relative path template under the account xPub
          example: "0/{index}"
        items:
          type: array
          items:
            $ref: "#/components/schemas/HdPoolAddress"
        source:
          $ref: "#/components/schemas/SettingsSource"
        parentOrgId:
          type: string
          format: uuid
          nullable: true
        effectiveOrgId:
          type: string
          format: uuid
    AddressSource:
      type: string
      enum: [main, hd_pool]
    AssetCode:
      type: string
      enum: [USDT, USDC, BTC, ETH, TRX]
    NetworkId:
      type: string
      enum:
        - ethereum
        - tron
        - tron_nile
        - bnb_smart_chain
        - polygon
        - arbitrum_one
        - solana
        - ton
        - base
        - bitcoin
      description: >
        Network id for payment orders. `tron_nile` is local/staging testnet only
        (CRYPTOGATE_CHAIN_ENV=testnet); never expose in production product builds.
    Money:
      type: object
      required: [amount, currency]
      properties:
        amount:
          type: string
          description: Decimal string in major units (no float)
          example: "50.01"
        currency:
          $ref: "#/components/schemas/AssetCode"
    Error:
      type: object
      required: [code, message]
      properties:
        code:
          type: string
        message:
          type: string
        details:
          type: object
          additionalProperties: true
          description: >
            Optional structured context. For `mode_b_amount_in_use` /
            `mode_d_memo_in_use` (409 on POST /orders), includes `blockingOrder`
            with id, orderNumber, status, payableAmount, asset, network,
            createdAt, createdByEmail, createdByLabel (and memoOrTag for Mode D).    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 12
        orgId:
          type: string
          description: Optional org scope when user has multiple memberships
    LoginResponse:
      type: object
      required: [session, mfaRequired]
      properties:
        session:
          $ref: "#/components/schemas/Session"
        mfaRequired:
          type: boolean
    MfaEnrollResponse:
      type: object
      required: [otpauthUrl, secret]
      properties:
        otpauthUrl:
          type: string
        secret:
          type: string
    MfaVerifyRequest:
      type: object
      required: [code]
      properties:
        code:
          type: string
          minLength: 6
          maxLength: 8
    Session:
      type: object
      required:
        - userId
        - email
        - memberships
        - mustChangePassword
        - mfaEnrolled
        - mfaEnforcement
        - sessionTimeoutMinutes
      properties:
        userId:
          type: string
        email:
          type: string
          format: email
        displayName:
          type: string
          nullable: true
          description: Optional display name (A10)
        locale:
          type: string
          description: UI language preference (A10)
          example: en
        timezone:
          type: string
          description: IANA timezone for date display (A10)
          example: UTC
        mustChangePassword:
          type: boolean
          description: When true, portals must block until POST /auth/change-password succeeds
        mfaEnrolled:
          type: boolean
          description: >
            True when the user completed TOTP MFA enrollment. Owner and
            Administrator portals must force enrollment when false (A5).
        mfaEnforcement:
          type: boolean
          description: >
            User preference. When true, Owner/Administrator portals force TOTP
            enrollment for this account until MFA is enabled.
        sessionTimeoutMinutes:
          type: integer
          enum: [15, 30, 60, 120]
          description: User preference — sliding session TTL in minutes
        memberships:
          type: array
          items:
            $ref: "#/components/schemas/OrgMembership"
    OrgAccount:
      type: object
      required: [id, type, name, parentId, status]
      properties:
        id:
          type: string
        type:
          $ref: "#/components/schemas/OrgType"
        name:
          type: string
        parentId:
          type: string
          nullable: true
        status:
          type: string
          enum: [active, paused]
          description: Operational gate for agent portal access
        orderCreateSuspended:
          type: boolean
          description: >
            When true, POST /orders is blocked by B7 suspend_order_create
            (merchant account may still be active).
        structure:
          type: string
          enum: [single_location, multi_location]
          description: Merchant accounts only
        createdAt:
          type: string
          format: date-time
          description: Org account creation time (onboard date)
        country:
          type: string
          description: Country captured at onboard
        billingEmail:
          type: string
          format: email
          description: Billing contact email captured at onboard
        legalName:
          type: string
          description: Legal entity name when different from display name
    CreateOrgRequest:
      type: object
      required: [type, name, parentId]
      properties:
        type:
          $ref: "#/components/schemas/OrgType"
        name:
          type: string
        parentId:
          type: string
        structure:
          type: string
          enum: [single_location, multi_location]
        commercial:
          $ref: "#/components/schemas/MerchantCommercialOnCreate"
          description: >
            Required when type is merchant. Agent assigns tier and volume fee
            within platform band; Enterprise custom rates may enter pending approval.
        commissionPercent:
          type: string
          description: >
            Optional when type is agent or agent_sub. Percent of platform fee
            paid to the agent (default 15). Applies immediately.
        country:
          type: string
          description: Country captured at onboard
        billingEmail:
          type: string
          format: email
          description: Billing contact email captured at onboard
        billingContact:
          type: string
          format: email
          description: Alias for billingEmail (merchant onboard forms)
        legalName:
          type: string
          description: Legal entity name when different from display name
    MerchantCommercialOnCreate:
      type: object
      required: [tier, volumeFeePercent]
      additionalProperties: false
      properties:
        tier:
          $ref: "#/components/schemas/MerchantTier"
        volumeFeePercent:
          type: string
          description: Percent in major units, e.g. "2.0"
          example: "2.0"
    InviteUserRequest:
      type: object
      required: [email, role]
      properties:
        email:
          type: string
          format: email
        role:
          $ref: "#/components/schemas/UserRole"
    InviteOrgUserResult:
      allOf:
        - $ref: "#/components/schemas/OrgMembership"
        - type: object
          properties:
            temporaryPassword:
              type: string
              nullable: true
              description: Present once when a new user was provisioned; copy for out-of-band share
            invitePath:
              type: string
              nullable: true
              description: Relative portal path for set-password invite link
            inviteUrl:
              type: string
              format: uri
              nullable: true
            emailDelivery:
              type: object
              properties:
                status:
                  type: string
                  enum: [stubbed, sent, skipped]
                mode:
                  type: string
    OrgMembership:
      type: object
      required: [orgId, userId, role, orgType, status]
      properties:
        orgId:
          type: string
        userId:
          type: string
        role:
          $ref: "#/components/schemas/UserRole"
        orgType:
          $ref: "#/components/schemas/OrgType"
        status:
          $ref: "#/components/schemas/MembershipStatus"
    OrgMember:
      allOf:
        - $ref: "#/components/schemas/OrgMembership"
        - type: object
          required: [email]
          properties:
            email:
              type: string
              format: email
            mfaEnrolled:
              type: boolean
              description: True when the user completed MFA enrollment (B15 team table).
            lastLoginAt:
              type: string
              format: date-time
              nullable: true
              description: Most recent session create time; null if never signed in.
    CreatePaymentOrderRequest:
      type: object
      required: [amount, asset, network, validitySeconds]
      additionalProperties: false
      description: >
        Do not send matchingMode, receiveAddress, or fee fields. Matching mode
        is the merchant default locked onto the order at create.
      properties:
        amount:
          type: string
          description: Requested amount before Mode C fingerprint adjustment
          example: "245.00"
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        validitySeconds:
          type: integer
          minimum: 60
          example: 900
        merchantReference:
          type: string
          maxLength: 200
          description: >
            Optional merchant PO, table, or invoice reference ("for what").
            Stored in merchant_metadata.reference; included in CSV export.
        merchantMetadata:
          type: object
          additionalProperties: true
    PaymentOrder:
      type: object
      description: >
        Shared payment-order row (API camelCase). DB columns are PaymentOrderColumn
        in @cryptogate/domain. Receive address is merchant-controlled.
      required:
        - id
        - orgId
        - orderNumber
        - status
        - matchingMode
        - payableAmount
        - receivedAmount
        - receiveAddress
        - addressSource
        - hdIndex
        - memoOrTag
        - asset
        - network
        - expiresAt
      properties:
        id:
          type: string
        orgId:
          type: string
          description: Merchant org that owns the payment order
        orderNumber:
          type: string
          example: "CG-2026-0042"
        status:
          $ref: "#/components/schemas/OrderStatus"
        matchingMode:
          $ref: "#/components/schemas/MatchingMode"
        payableAmount:
          $ref: "#/components/schemas/Money"
        receivedAmount:
          $ref: "#/components/schemas/Money"
          nullable: true
        receiveAddress:
          type: string
          description: Merchant-controlled; immutable after QR is issued
        addressSource:
          $ref: "#/components/schemas/AddressSource"
        hdIndex:
          type: integer
          nullable: true
          description: Set when addressSource is hd_pool (Mode S)
        memoOrTag:
          type: string
          nullable: true
          description: Set for Mode D when the network supports memos
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        expiresAt:
          type: string
          format: date-time
        createdAt:
          type: string
          format: date-time
        createdBy:
          type: string
        createdByEmail:
          type: string
          format: email
          nullable: true
        orgName:
          type: string
          nullable: true
          description: Merchant or merchant (site) org display name
        merchantReference:
          type: string
          nullable: true
          description: Merchant PO / purpose reference at create
        fulfillmentPolicy:
          $ref: "#/components/schemas/FulfillmentPolicy"
          description: When staff may release goods; stamped at create
        anomalyReason:
          type: string
          nullable: true
          description: >
            Set when status is payment_anomaly (e.g. mode_b_same_amount_collision,
            mode_b_underpay, wrong_network). Kept after resolve for invoice/audit.
        anomalyResolutionNote:
          type: string
          nullable: true
          description: Staff note when anomaly was resolved (manual reconcile).
        anomalyResolvedAt:
          type: string
          format: date-time
          nullable: true
          description: When staff resolved the anomaly (order then cancelled).
    PaymentOrderList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/PaymentOrder"
    PaymentDetails:
      type: object
      description: Guest pay / POS QR payload. Never includes keys, xPub, or fees.
      required:
        - orderNumber
        - status
        - merchantName
        - matchingMode
        - paymentPageUrl
        - qrPayload
        - receiveAddress
        - payableAmount
        - copyAmount
        - asset
        - network
        - expiresAt
        - wrongNetworkWarning
      properties:
        orderNumber:
          type: string
          example: "CG-2026-0042"
        status:
          $ref: "#/components/schemas/OrderStatus"
        merchantName:
          type: string
          example: "Hotel Marrakech — Casablanca"
        matchingMode:
          $ref: "#/components/schemas/MatchingMode"
        paymentPageUrl:
          type: string
          format: uri
        qrPayload:
          type: string
          description: >
            Payload encoded in the guest/POS QR. Phase 1 uses the HTTPS payment
            page URL so a phone camera opens amount, asset, network, and address.
            Not a chain RPC URL.
        walletUri:
          type: string
          description: >
            Optional network URI hint (`{network}:{address}?amount&asset`) for
            wallets that parse it. Prefer qrPayload (pay page) for camera scan.
        receiveAddress:
          type: string
        payableAmount:
          $ref: "#/components/schemas/Money"
        copyAmount:
          type: string
          description: Copy-ready payable amount (major units, no asset suffix)
          example: "245.00"
        asset:
          $ref: "#/components/schemas/AssetCode"
        network:
          $ref: "#/components/schemas/NetworkId"
        contractAddress:
          type: string
          nullable: true
        memoOrTag:
          type: string
          nullable: true
        expiresAt:
          type: string
          format: date-time
        wrongNetworkWarning:
          type: string
          example: "Send only USDT on TRON TRC-20. Wrong network may result in lost funds."
        confirmations:
          type: integer
          minimum: 0
          description: Watcher confirmation count (guest progress UI)
        requiredConfirmations:
          type: integer
          minimum: 1
          description: Network-required confirmations for this order
        txHash:
          type: string
          nullable: true
          description: Bound tx hash when detected (no explorer secrets)
        createdAt:
          type: string
          format: date-time
          description: Order create time for guest timeline
        confirmedAt:
          type: string
          format: date-time
          nullable: true
          description: Set when watcher marks confirmations complete
        anomalyReason:
          type: string
          nullable: true
          description: Present when status is payment_anomaly
        payExactAmountWarning:
          type: string
          nullable: true
          description: Present for Mode C fingerprint orders
        memoWarning:
          type: string
          nullable: true
          description: Present for Mode D when memoOrTag is required
    OnChainDetails:
      type: object
      description: >
        Watcher-written facts (Andrew GET /orders/{id}/on-chain). Unseen tx
        fields are null; toAddress is the order receive address even before a
        match. Do not invent blockHeight, fromAddress, or confirmedAt.
      properties:
        txHash:
          type: string
          nullable: true
        blockHeight:
          type: integer
          nullable: true
        fromAddress:
          type: string
          nullable: true
        toAddress:
          type: string
          nullable: true
        amount:
          $ref: "#/components/schemas/Money"
        confirmedAt:
          type: string
          format: date-time
          nullable: true
    NotificationEventType:
      type: string
      enum:
        - payment_completed
        - payment_anomaly
        - settlement_address
        - xpub_change
        - webhook_failures
        - service_bills
        - site_overrides
    NotificationPreference:
      type: object
      required: [eventType, email, inApp]
      properties:
        eventType:
          $ref: "#/components/schemas/NotificationEventType"
        email:
          type: boolean
        inApp:
          type: boolean
    NotificationPreferenceList:
      type: object
      required: [items, emailAvailable]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/NotificationPreference"
        emailAvailable:
          type: boolean
          description: False when outbound SMTP is not configured; email toggles are disabled.
    UpdateNotificationPreferencesRequest:
      type: object
      required: [items]
      properties:
        items:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/NotificationPreference"
    WebhookEventType:

      type: string
      description: Matches @cryptogate/domain WebhookEventType
      enum:
        - payment_order.created
        - payment_order.verifying
        - payment_order.completed
        - payment_order.expired
        - payment_order.payment_anomaly
        - payment_order.failed
        - webhook.test
    ApiKeyScope:
      type: string
      enum: [orders, webhooks]
      description: Machine-key capability scope (D14)
    CreateApiKeyRequest:
      type: object
      required: [label]
      properties:
        label:
          type: string
          minLength: 1
          maxLength: 64
          description: Human label in merchant portal (not the keyId)
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: Optional expiry; null = no expiry. Rejected if in the past.
        scopes:
          type: array
          minItems: 1
          maxItems: 2
          items:
            $ref: "#/components/schemas/ApiKeyScope"
          description: Defaults to both orders and webhooks when omitted
        ipAllowlist:
          type: array
          maxItems: 32
          items:
            type: string
            minLength: 1
            maxLength: 64
          description: Empty or omitted = any IP. Exact IPv4/IPv6 or IPv4 CIDR.
    RotateApiKeyRequest:
      type: object
      properties:
        expiresAt:
          type: string
          format: date-time
          nullable: true
          description: >
            Optional new expiry for the replacement key. Omit to keep the
            previous key's expiresAt (including null).
        scopes:
          type: array
          minItems: 1
          maxItems: 2
          items:
            $ref: "#/components/schemas/ApiKeyScope"
        ipAllowlist:
          type: array
          maxItems: 32
          items:
            type: string
    ApiKey:
      type: object
      required: [id, orgId, keyId, label, createdAt, lastUsedAt, expiresAt, scopes, ipAllowlist]
      description: >
        Public API key metadata — never `secret`. Matches @cryptogate/domain ApiKey.
      properties:
        id:
          type: string
          format: uuid
        orgId:
          type: string
          format: uuid
        keyId:
          type: string
          minLength: 8
          maxLength: 128
          description: Public id sent as X-Api-Key (prefix cgk_)
          example: cgk_live_1a2b3c4d5e6f
        label:
          type: string
        createdAt:
          type: string
          format: date-time
        lastUsedAt:
          type: string
          format: date-time
          nullable: true
        expiresAt:
          type: string
          format: date-time
          nullable: true
        scopes:
          type: array
          items:
            $ref: "#/components/schemas/ApiKeyScope"
        ipAllowlist:
          type: array
          items:
            type: string
    ApiKeyList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          maxItems: 10
          items:
            $ref: "#/components/schemas/ApiKey"
    ApiKeyCreated:
      allOf:
        - $ref: "#/components/schemas/ApiKey"
        - type: object
          required: [secret]
          properties:
            secret:
              type: string
              minLength: 32
              description: >
                HMAC secret shown once on create/rotate; store hashed at rest;
                never log; never return on GET
    WebhookDeliveryStatus:
      type: string
      enum: [pending, success, failed]
    RegisterWebhookRequest:
      type: object
      required: [url]
      properties:
        url:
          type: string
          format: uri
        events:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEventType"
          description: Default all payment_order.* (not webhook.test)
    WebhookEndpoint:
      type: object
      required: [id, orgId, url, events, enabled]
      description: Public endpoint row — never signingSecret
      properties:
        id:
          type: string
          format: uuid
        orgId:
          type: string
        url:
          type: string
          format: uri
        events:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEventType"
        enabled:
          type: boolean
        createdAt:
          type: string
          format: date-time
    WebhookEndpointList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WebhookEndpoint"
    WebhookCreated:
      allOf:
        - $ref: "#/components/schemas/WebhookEndpoint"
        - type: object
          required: [signingSecret]
          properties:
            signingSecret:
              type: string
              minLength: 32
              description: Shown once on create; store hashed at rest; never log
    TestWebhookRequest:
      type: object
      properties:
        webhookId:
          type: string
          format: uuid
    WebhookTestAccepted:
      type: object
      required: [queued]
      properties:
        queued:
          type: integer
          minimum: 0
    WebhookEvent:
      type: object
      required: [id, type, createdAt, data]
      description: Body POSTed to the merchant URL (HMAC over raw JSON bytes)
      properties:
        id:
          type: string
          format: uuid
          description: eventId — retries keep this id
        type:
          $ref: "#/components/schemas/WebhookEventType"
        createdAt:
          type: string
          format: date-time
        data:
          type: object
          additionalProperties: true
          properties:
            orderId:
              type: string
            orderNumber:
              type: string
            status:
              $ref: "#/components/schemas/OrderStatus"
    WebhookDelivery:
      type: object
      required: [id, eventId, status, attempt]
      properties:
        id:
          type: string
          format: uuid
        eventId:
          type: string
          format: uuid
        status:
          $ref: "#/components/schemas/WebhookDeliveryStatus"
        attempt:
          type: integer
          minimum: 1
        httpStatus:
          type: integer
          nullable: true
        nextRetryAt:
          type: string
          format: date-time
          nullable: true
    WebhookDeliveryList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WebhookDelivery"
    ServiceBillStatus:
      type: string
      description: Matches @cryptogate/domain ServiceBillStatus
      enum: [issued, paid, overdue, voided]
    ServiceBill:
      type: object
      required:
        - id
        - orgId
        - periodStart
        - periodEnd
        - subscriptionAmount
        - volumeFeeAmount
        - totalAmount
        - currency
        - status
        - dueAt
      description: >
        Platform SaaS invoice — not a payment order. Tier, volume fee %, and
        billed volume are frozen at issue for accountant-stable math.
      properties:
        id:
          type: string
          format: uuid
        orgId:
          type: string
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        subscriptionAmount:
          type: string
          example: "49.00"
        volumeFeeAmount:
          type: string
          example: "12.50"
        totalAmount:
          type: string
          example: "61.50"
        currency:
          type: string
          enum: [USD]
        status:
          $ref: "#/components/schemas/ServiceBillStatus"
        dueAt:
          type: string
          format: date-time
        tier:
          type: string
          nullable: true
          enum: [small, mid, enterprise]
          description: Fee tier snapshot at issue
        volumeFeePercent:
          type: string
          nullable: true
          description: Effective volume fee percent snapshot at issue
          example: "1.20"
        billedVolumeUsd:
          type: string
          nullable: true
          description: Confirmed completed payment-order volume used for the fee line
          example: "1000.00"
        paidAt:
          type: string
          format: date-time
          nullable: true
          description: Set when status is paid (v0.3.2)
        voidedAt:
          type: string
          format: date-time
          nullable: true
          description: Set when status is voided (v0.3.2)
        lastAdjustmentReason:
          type: string
          nullable: true
          description: Most recent platform adjustment note (v0.3.2)
        lastAdjustmentAmount:
          type: string
          nullable: true
          description: Signed USD delta last applied to totalAmount (e.g. "-10.00")
          example: "-10.00"
        paymentReference:
          type: string
          nullable: true
          description: Tx hash / bank remittance reference when marked paid
        rxAddress:
          type: string
          nullable: true
          description: Platform billing wallet (receive) snapshotted at mark-paid
        remittancePayTo:
          type: string
          nullable: true
          description: >
            Effective remittance destination for invoice / pay card display —
            rxAddress when set, otherwise live platform billing wallet payTo.
        txAddress:
          type: string
          nullable: true
          description: Merchant payer / sender address when marked paid
        createdAt:
          type: string
          format: date-time
          nullable: true
          description: Issue timestamp
    ServiceBillList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ServiceBill"
    IssueServiceBillRequest:
      type: object
      required: [orgId, periodStart, periodEnd, subscriptionAmount, volumeFeeAmount, dueAt]
      properties:
        orgId:
          type: string
          format: uuid
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        subscriptionAmount:
          type: string
        volumeFeeAmount:
          type: string
        dueAt:
          type: string
          format: date-time
        tier:
          type: string
          enum: [small, mid, enterprise]
          description: Optional; resolved from merchant commercial at issue when omitted
        volumeFeePercent:
          type: string
          description: Optional snapshot; resolved from commercial when omitted
        billedVolumeUsd:
          type: string
          description: Optional snapshot; computed from completed volume when omitted
    GenerateServiceBillsRequest:
      type: object
      description: Omit both dates to generate the previous UTC calendar month.
      properties:
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
    GenerateServiceBillsResult:
      type: object
      required: [periodStart, periodEnd, issued, skipped]
      properties:
        periodStart:
          type: string
          format: date
        periodEnd:
          type: string
          format: date
        issued:
          type: array
          items:
            $ref: "#/components/schemas/ServiceBill"
        skipped:
          type: array
          items:
            type: object
            required: [orgId, reason]
            properties:
              orgId:
                type: string
                format: uuid
              reason:
                type: string
                enum: [already_issued, no_commercial, no_fee_tier]
    UpdateServiceBillRequest:
      type: object
      required: [action]
      additionalProperties: false
      properties:
        action:
          type: string
          enum: [mark_paid, void, adjust]
          description: Matches @cryptogate/domain ServiceBillUpdateAction
        reason:
          type: string
          minLength: 1
          maxLength: 500
          description: Required for void and adjust
        adjustmentAmount:
          type: string
          description: >
            USD delta applied to totalAmount when action=adjust (signed decimal
            string, e.g. "-10.00" or "5.50"). Required for adjust.
        paymentReference:
          type: string
          maxLength: 256
          description: Optional tx hash / bank remittance reference when action=mark_paid
        rxAddress:
          type: string
          maxLength: 128
          description: >
            Optional receive address snapshot when action=mark_paid. When omitted,
            API fills from platform billing wallet payTo if configured.
        txAddress:
          type: string
          maxLength: 128
          description: Optional merchant payer / sender address when action=mark_paid
    ServiceBillCheckout:
      type: object
      required: [billId, totalAmount, currency, payTo, instructions]
      description: Not PaymentDetails; not served by apps/payment-page
      properties:
        billId:
          type: string
          format: uuid
        totalAmount:
          type: string
        currency:
          type: string
          enum: [USD]
        payTo:
          type: string
          description: Platform billing wallet or bank instructions
        qrPayload:
          type: string
          nullable: true
        instructions:
          type: string
    MerchantTier:
      type: string
      description: Matches @cryptogate/domain MerchantTier
      enum: [small, mid, enterprise]
    FeeTierBand:
      type: object
      required:
        - tier
        - subscriptionAmountUsd
        - volumeFeeMinPercent
        - volumeFeeMaxPercent
        - defaultSignupPercent
      properties:
        tier:
          $ref: "#/components/schemas/MerchantTier"
        subscriptionAmountUsd:
          type: string
          description: Monthly subscription in USD major units. Enterprise may be negotiated per contract.
          example: "49.00"
        volumeFeeMinPercent:
          type: string
          example: "1.2"
        volumeFeeMaxPercent:
          type: string
          example: "2.0"
        defaultSignupPercent:
          type: string
          description: Default rate at merchant signup (within min–max)
          example: "2.0"
        tierDescription:
          type: string
          description: Editable help text for platform UI (B8)
    PlatformFeeTierSettings:
      type: object
      required: [tiers, updatedAt]
      properties:
        tiers:
          type: array
          minItems: 3
          maxItems: 3
          items:
            $ref: "#/components/schemas/FeeTierBand"
        updatedAt:
          type: string
          format: date-time
    UpdatePlatformFeeTierSettingsRequest:
      type: object
      required: [tiers]
      additionalProperties: false
      properties:
        tiers:
          type: array
          minItems: 3
          maxItems: 3
          items:
            $ref: "#/components/schemas/FeeTierBand"
    PlatformOrgPolicy:
      type: object
      required: [maxAgentDepth, mfaEnforcement, sessionTimeoutMinutes]
      properties:
        maxAgentDepth:
          type: integer
          minimum: 0
          maximum: 5
          description: Phase 1 default 2
          example: 2
        mfaEnforcement:
          type: boolean
          description: >
            When true, Owner/Administrator portals force TOTP enrollment (A5).
            Privileged actions still require enrolled MFA regardless.
        sessionTimeoutMinutes:
          type: integer
          enum: [15, 30, 60, 120]
          description: Sliding session TTL applied on login and session refresh
    PlatformBillingWalletSettings:
      type: object
      required: [sellerName, sellerEmail, payTo, updatedAt]
      description: >
        B11-lite platform invoice seller + service-bill remittance destination.
        Distinct from merchant settlement addresses.
      properties:
        sellerName:
          type: string
          example: CryptoGate
        sellerEmail:
          type: string
          format: email
          nullable: true
        payTo:
          type: string
          nullable: true
          description: >
            Wallet address or remittance instruction for merchant→platform
            service-bill payments. Checkout prefers this over PLATFORM_BILLING_PAY_TO.
        updatedAt:
          type: string
          format: date-time
    UpdatePlatformBillingWalletSettingsRequest:
      type: object
      required: [sellerName]
      additionalProperties: false
      properties:
        sellerName:
          type: string
          minLength: 1
          maxLength: 200
        sellerEmail:
          type: string
          format: email
          nullable: true
        payTo:
          type: string
          maxLength: 500
          nullable: true
    UpdatePlatformOrgPolicyRequest:
      type: object
      required: [maxAgentDepth, mfaEnforcement, sessionTimeoutMinutes]
      additionalProperties: false
      properties:
        maxAgentDepth:
          type: integer
          minimum: 0
          maximum: 5
        mfaEnforcement:
          type: boolean
        sessionTimeoutMinutes:
          type: integer
          enum: [15, 30, 60, 120]
    MerchantCommercialSettings:
      type: object
      required:
        - orgId
        - tier
        - volumeFeePercent
        - subscriptionAmountUsd
        - bandMinPercent
        - bandMaxPercent
        - effectiveFrom
      properties:
        orgId:
          type: string
          format: uuid
        tier:
          $ref: "#/components/schemas/MerchantTier"
        volumeFeePercent:
          type: string
          description: Effective rate for current billing period
          example: "2.0"
        pendingVolumeFeePercent:
          type: string
          nullable: true
          description: Legacy scheduled rate from prior next-period behavior; new updates apply immediately
        subscriptionAmountUsd:
          type: string
          example: "49.00"
        bandMinPercent:
          type: string
          description: Platform band for current tier (UI slider bounds)
        bandMaxPercent:
          type: string
        effectiveFrom:
          type: string
          format: date
          description: Start of billing period this rate applies to
        enterpriseApprovalStatus:
          $ref: "#/components/schemas/EnterpriseRateApprovalStatus"
          nullable: true
          description: Set when Enterprise custom rate awaits or completed platform review
    UpdateMerchantCommercialRequest:
      type: object
      additionalProperties: false
      properties:
        tier:
          $ref: "#/components/schemas/MerchantTier"
        volumeFeePercent:
          type: string
        reason:
          type: string
          description: Optional note; logged to audit
    EnterpriseRateApprovalStatus:
      type: string
      enum: [pending, approved, denied]
    EnterpriseRateApproval:
      type: object
      required:
        - id
        - orgId
        - merchantName
        - requestedTier
        - requestedVolumeFeePercent
        - status
        - requestedByUserId
        - createdAt
      properties:
        id:
          type: string
          format: uuid
        orgId:
          type: string
          format: uuid
        merchantName:
          type: string
        requestedTier:
          $ref: "#/components/schemas/MerchantTier"
        requestedVolumeFeePercent:
          type: string
        status:
          $ref: "#/components/schemas/EnterpriseRateApprovalStatus"
        requestedByUserId:
          type: string
          format: uuid
        decidedByUserId:
          type: string
          format: uuid
          nullable: true
        decisionReason:
          type: string
          nullable: true
        createdAt:
          type: string
          format: date-time
        decidedAt:
          type: string
          format: date-time
          nullable: true
    EnterpriseRateApprovalList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/EnterpriseRateApproval"
    WatcherHealthStatus:
      type: string
      enum: [ok, degraded, down, unknown]
    WatcherHeartbeat:
      type: object
      required:
        - network
        - asset
        - tick
        - status
        - healthScore
        - rpcOk
        - rpcMode
        - ingestMode
        - pollIntervalMs
        - openOrders
        - awaitingConfirmations
        - transfersSeen
        - tickAt
        - updatedAt
        - lagMs
      properties:
        network:
          type: string
          example: tron
        asset:
          type: string
          example: USDT
        tick:
          type: integer
        status:
          $ref: "#/components/schemas/WatcherHealthStatus"
        healthScore:
          type: integer
          minimum: 0
          maximum: 100
        rpcOk:
          type: boolean
        rpcMode:
          type: string
          description: trongrid | eth-rpc | stub
        ingestMode:
          type: string
          description: match+confirm | noop | error | idle
        pollIntervalMs:
          type: integer
        openOrders:
          type: integer
        awaitingConfirmations:
          type: integer
        transfersSeen:
          type: integer
        lastError:
          type: string
          nullable: true
        detail:
          type: object
          additionalProperties: true
        tickAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        lagMs:
          type: integer
          description: Milliseconds since tickAt (computed on read)
    WatcherHealthList:
      type: object
      required: [items, checkedAt]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/WatcherHeartbeat"
        checkedAt:
          type: string
          format: date-time
        note:
          type: string
          description: Optional operator hint (e.g. missing migration)
    NetworkOrderabilityLamp:
      type: object
      description: >
        Single-signal orderability — Open (green), Paused (amber), Down (red),
        Off (grey). Combines catalog enabled + maintenance + watcher ingest.
      required: [code, label, tone]
      properties:
        code:
          type: string
          enum: [open, paused, down, off]
        label:
          type: string
          enum: [Open, Paused, Down, Off]
        tone:
          type: string
          enum: [ok, warn, bad, muted]
    NetworkCatalogIngestStatus:
      type: string
      enum: [live, stub, degraded, down, unknown]
    NetworkCatalogCardStatus:
      type: string
      enum: [active, maintenance, catalogued]
    NetworkCatalogIngest:
      type: object
      required:
        - ingestStatus
        - ingestLabel
        - rpcConfigured
        - openOrders
      properties:
        ingestStatus:
          $ref: "#/components/schemas/NetworkCatalogIngestStatus"
        ingestLabel:
          type: string
        rpcConfigured:
          type: boolean
        rpcMode:
          type: string
          nullable: true
        healthScore:
          type: integer
          nullable: true
          minimum: 0
          maximum: 100
          description: Watcher heartbeat score when present; null if no heartbeat
        lagMs:
          type: integer
          nullable: true
        tickAt:
          type: string
          format: date-time
          nullable: true
        openOrders:
          type: integer
    NetworkCatalogPair:
      type: object
      required:
        - asset
        - enabled
        - decimals
        - minAmount
        - requiredConfirmations
        - displayNetwork
        - lamp
      properties:
        asset:
          $ref: "#/components/schemas/AssetCode"
        enabled:
          type: boolean
        contractAddress:
          type: string
          nullable: true
        decimals:
          type: integer
        minAmount:
          type: string
        requiredConfirmations:
          type: integer
        displayNetwork:
          type: string
        lamp:
          $ref: "#/components/schemas/NetworkOrderabilityLamp"
    NetworkCatalogCard:
      type: object
      required:
        - network
        - title
        - status
        - lamp
        - pairCount
        - enabledCount
        - catalogFraction
        - pairs
        - maintenance
        - ingest
      properties:
        network:
          $ref: "#/components/schemas/NetworkId"
        title:
          type: string
        status:
          $ref: "#/components/schemas/NetworkCatalogCardStatus"
        lamp:
          $ref: "#/components/schemas/NetworkOrderabilityLamp"
        pairCount:
          type: integer
        enabledCount:
          type: integer
        catalogFraction:
          type: number
          description: enabledCount / pairCount (registry completeness)
        primaryAsset:
          type: string
          nullable: true
        confirmations:
          type: integer
          nullable: true
        minAmount:
          type: string
          nullable: true
        contractAddress:
          type: string
          nullable: true
        pairs:
          type: array
          items:
            $ref: "#/components/schemas/NetworkCatalogPair"
        maintenance:
          type: object
          required: [active]
          properties:
            active:
              type: boolean
            message:
              type: string
              nullable: true
            startedAt:
              type: string
              format: date-time
              nullable: true
            endsAt:
              type: string
              format: date-time
              nullable: true
            updatedAt:
              type: string
              format: date-time
              nullable: true
        ingest:
          $ref: "#/components/schemas/NetworkCatalogIngest"
    NetworkCatalog:
      type: object
      required: [chainEnv, checkedAt, items]
      properties:
        chainEnv:
          type: string
          enum: [mainnet, testnet]
        checkedAt:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/NetworkCatalogCard"
    NetworksStatus:
      type: object
      required: [chainEnv, checkedAt, items]
      properties:
        chainEnv:
          type: string
          enum: [mainnet, testnet]
        checkedAt:
          type: string
          format: date-time
        items:
          type: array
          items:
            $ref: "#/components/schemas/NetworksStatusItem"
    NetworksStatusItem:
      type: object
      required: [network, title, lamp, maintenance, ingestStatus, pairs]
      properties:
        network:
          $ref: "#/components/schemas/NetworkId"
        title:
          type: string
        lamp:
          $ref: "#/components/schemas/NetworkOrderabilityLamp"
        maintenance:
          type: object
          required: [active]
          properties:
            active:
              type: boolean
            message:
              type: string
              nullable: true
        ingestStatus:
          $ref: "#/components/schemas/NetworkCatalogIngestStatus"
        pairs:
          type: array
          items:
            type: object
            required: [asset, enabled, lamp, displayNetwork]
            properties:
              asset:
                $ref: "#/components/schemas/AssetCode"
              enabled:
                type: boolean
              lamp:
                $ref: "#/components/schemas/NetworkOrderabilityLamp"
              displayNetwork:
                type: string
    NetworkMaintenanceUpdate:
      type: object
      required: [active]
      additionalProperties: false
      properties:
        active:
          type: boolean
        message:
          type: string
          maxLength: 500
          nullable: true
          description: Merchant-facing banner; defaulted when active and omitted
        endsAt:
          type: string
          format: date-time
          nullable: true
    NetworkMaintenanceState:
      type: object
      required: [network, active, updatedAt]
      properties:
        network:
          $ref: "#/components/schemas/NetworkId"
        active:
          type: boolean
        message:
          type: string
          nullable: true
        startedAt:
          type: string
          format: date-time
          nullable: true
        endsAt:
          type: string
          format: date-time
          nullable: true
        updatedAt:
          type: string
          format: date-time
    ActiveNetworkMaintenanceList:
      type: object
      required: [items, checkedAt]
      properties:
        items:
          type: array
          items:
            type: object
            required: [network]
            properties:
              network:
                $ref: "#/components/schemas/NetworkId"
              message:
                type: string
                nullable: true
              startedAt:
                type: string
                format: date-time
                nullable: true
              endsAt:
                type: string
                format: date-time
                nullable: true
        checkedAt:
          type: string
          format: date-time
        note:
          type: string
    ComplianceOverrideType:
      type: string
      enum:
        - settlement_address
        - matching_mode
        - suspend_order_create
        - suspend_merchant
    ComplianceReasonCode:
      type: string
      enum:
        - manual_review
        - suspicious_activity
        - sanctions_screening
        - other
    ComplianceOverrideRequest:
      type: object
      required: [overrideType, reasonCode, notes, mfaCode]
      additionalProperties: false
      properties:
        overrideType:
          $ref: "#/components/schemas/ComplianceOverrideType"
        reasonCode:
          $ref: "#/components/schemas/ComplianceReasonCode"
        notes:
          type: string
          minLength: 8
          maxLength: 2000
        ticketId:
          type: string
          maxLength: 120
        mfaCode:
          type: string
          minLength: 6
          maxLength: 8
        matchingMode:
          type: string
          enum: [B, C, D, S]
          description: Required when overrideType is matching_mode
        settlement:
          type: object
          required: [asset, network, address]
          additionalProperties: false
          description: Required when overrideType is settlement_address
          properties:
            asset:
              $ref: "#/components/schemas/AssetCode"
            network:
              $ref: "#/components/schemas/NetworkId"
            address:
              type: string
    ComplianceOverride:
      type: object
      required:
        - id
        - orgId
        - actorUserId
        - overrideType
        - reasonCode
        - notes
        - createdAt
      properties:
        id:
          type: string
        orgId:
          type: string
        actorUserId:
          type: string
        overrideType:
          $ref: "#/components/schemas/ComplianceOverrideType"
        reasonCode:
          $ref: "#/components/schemas/ComplianceReasonCode"
        notes:
          type: string
        ticketId:
          type: string
          nullable: true
        metadata:
          type: object
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
    ComplianceOverrideList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ComplianceOverride"
        softEmpty:
          type: boolean
          description: True when migration 028 is not applied yet
    ComplianceOverrideResult:
      type: object
      required: [override]
      properties:
        override:
          $ref: "#/components/schemas/ComplianceOverride"
        org:
          $ref: "#/components/schemas/OrgAccount"
        matchingMode:
          $ref: "#/components/schemas/MatchingModeSettings"
        settlement:
          $ref: "#/components/schemas/SettlementAddress"
    PlatformOrgMemberEmailRow:
      type: object
      required: [orgId, emails]
      properties:
        orgId:
          type: string
        emails:
          type: array
          items:
            type: string
            format: email
        ownerEmail:
          type: string
          format: email
          nullable: true
          description: Owner-role email when the org has an Owner membership
    PlatformOrgMemberEmailList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/PlatformOrgMemberEmailRow"
    OrgOverview:
      type: object
      required: [team, audit, orders]
      properties:
        team:
          type: array
          items:
            $ref: "#/components/schemas/OrgMember"
        audit:
          type: array
          items:
            $ref: "#/components/schemas/AuditLogEntry"
        orders:
          type: array
          items:
            $ref: "#/components/schemas/PaymentOrder"
        commercial:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/MerchantCommercialSettings"
        payout:
          nullable: true
          allOf:
            - $ref: "#/components/schemas/AgentPayoutAddress"
    DecideEnterpriseRateApprovalRequest:
      type: object
      required: [decision]
      additionalProperties: false
      properties:
        decision:
          type: string
          enum: [approve, deny]
        reason:
          type: string
          description: Required when decision is deny
    AuditAction:
      type: string
      description: Matches @cryptogate/domain AuditAction
      enum:
        - login
        - logout
        - mfa_enroll
        - mfa_verify_enroll
        - mfa_verify_login
        - org_create
        - org_status
        - org_delete
        - org_user_invite
        - org_user_role
        - org_user_pause
        - org_user_resume
        - org_user_remove
        - settlement_put
        - matching_mode_put
        - xpub_put
        - webhook_register
        - webhook_delete
        - service_bill_issue
        - service_bill_mark_paid
        - service_bill_void
        - service_bill_adjust
        - api_key_create
        - api_key_revoke
        - api_key_rotate
        - fee_tier_put
        - org_policy_put
        - billing_wallet_put
        - merchant_commercial_put
        - enterprise_rate_decide
        - compliance_override
        - site_override_request
        - site_override_decide
    AuditLogEntry:
      type: object
      required: [id, action, metadata, createdAt]
      description: Append-only audit row; metadata redacted (no secrets)
      properties:
        id:
          type: string
          format: uuid
        actorUserId:
          type: string
          format: uuid
          nullable: true
        orgId:
          type: string
          format: uuid
          nullable: true
        action:
          $ref: "#/components/schemas/AuditAction"
        metadata:
          type: object
          additionalProperties:
            oneOf:
              - type: string
              - type: number
              - type: boolean
              - type: "null"
        createdAt:
          type: string
          format: date-time
    AuditLogList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/AuditLogEntry"
