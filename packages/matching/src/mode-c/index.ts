/**
 * Mode C — amount fingerprint (unique payable among open orders).
 * M1-32 scaffold only; implement in M2-41.
 */
import type { AssignInput, AssignResult } from "../types.js";

export async function assignModeC(_input: AssignInput): Promise<AssignResult> {
  throw new Error("assignModeC not implemented — Bruce M2-41");
}
